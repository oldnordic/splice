---
phase: 12-rich-span-advanced
plan: 06
type: execute
wave: 3
depends_on: [12-01, 12-05]
files_modified:
  - tests/relationship_performance.rs
  - Cargo.toml
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Relationship queries complete in reasonable time on 10K+ LOC codebase"
    - "Caller threshold (100) prevents runaway queries"
    - "Session caching works within single invocation"
    - "Performance regression test catches slowdowns"
  artifacts:
    - path: "tests/relationship_performance.rs"
      provides: "Performance tests for relationship queries"
      min_lines: 100
  key_links:
    - from: "tests/relationship_performance.rs"
      to: "src/relationships/mod.rs"
      via: "Tests call get_callers, get_callees, get_imports, get_exports"
      pattern: "relationships::"
---

<objective>
Implement performance testing for relationship queries on large codebases (>10K LOC) to validate lazy evaluation and caller threshold strategies.

Purpose: Ensure relationship queries scale efficiently and don't degrade performance for large codebases.
Output: Performance test suite with benchmarks for relationship operations.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 11 summaries
@.planning/phases/11-rich-span-core/11-01-SUMMARY.md

# Codebase reference
@src/relationships/mod.rs (from 12-01)
@src/graph/mod.rs
Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create performance test infrastructure</name>
  <files>tests/relationship_performance.rs</files>
  <action>
    Create tests/relationship_performance.rs with:

    1. Setup helper to create test code graph with varying sizes:
       - small_graph(): 100 symbols, ~10 files
       - medium_graph(): 1000 symbols, ~50 files
       - large_graph(): 10000 symbols, ~200 files

    2. Test fixtures for:
       - Function call chains (A -> B -> C)
       - Cross-file imports
       - Public exports
       - Circular dependencies (A -> B -> A)

    Use tempfile crate for temporary databases. Clean up after each test.
  </action>
  <verify>cargo test --test relationship_performance compiles</verify>
  <done>Performance test infrastructure with 3 graph sizes</done>
</task>

<task type="auto">
  <name>Task 2: Implement relationship query performance tests</name>
  <files>tests/relationship_performance.rs</files>
  <action>
    Add performance tests:

    1. test_get_callers_small_graph
       - Expect: < 10ms for 100 symbols

    2. test_get_callers_large_graph
       - Expect: < 100ms for 10000 symbols

    3. test_get_callees_large_graph
       - Expect: < 100ms for 10000 symbols

    4. test_threshold_enforcement
       - Create symbol with 150 callers
       - Verify get_callers returns empty vec (threshold enforced)

    5. test_imports_exports_performance
       - Expect: < 50ms for file with 500 imports

    Use std::time::Instant for timing. Assert upper bounds with reasonable margins.
  </action>
  <verify>cargo test --test relationship_performance -- --nocapture passes</verify>
  <done>Performance tests verify relationship query scalability</done>
</task>

<task type="auto">
  <name>Task 3: Add session caching test</name>
  <files>tests/relationship_performance.rs</files>
  <action>
    Add test for session-based caching:

    1. test_session_caching
       - Query same relationship twice in same "session"
       - Verify second query is faster (cached result)
       - Verify cache clears after operation completes

    Note: Session caching may not be implemented in 12-01. If so, this test should document the TODO and be marked as #[ignore].

    The test should verify the caching contract even if not yet implemented.
  </action>
  <verify>cargo test --test relationship_performance passes</verify>
  <done>Session caching test documents caching expectations</done>
</task>

<task type="auto">
  <name>Task 4: Add circular dependency detection test</name>
  <files>tests/relationship_performance.rs</files>
  <action>
    Add test for circular dependency handling:

    1. test_circular_dependency_detection
       - Create A -> B -> C -> A cycle
       - Query relationships for A
       - Verify cycle_detected: true in Relationships struct
       - Verify all relationships still returned (no infinite loop)

    2. test_deep_chain_handling
       - Create A -> B -> C -> ... -> Z chain (26 deep)
       - Query relationships for A
       - Verify only direct relationships returned (B only, not transitive)
       - Verify no stack overflow or timeout
  </action>
  <verify>cargo test --test relationship_performance passes</verify>
  <done>Circular dependency tests verify robustness</done>
</task>

</tasks>

<verification>
Overall phase checks:
- [ ] cargo test --test relationship_performance compiles
- [ ] All performance tests pass
- [ ] Small graph queries complete in < 10ms
- [ ] Large graph queries complete in < 100ms
- [ ] Caller threshold (100) enforced in tests
- [ ] Circular dependencies handled correctly
- [ ] Session caching test documents expectations
- [ ] No performance regressions introduced
</verification>

<success_criteria>
Measurable completion:
- tests/relationship_performance.rs exists with >100 lines
- test_get_callers_small_graph verifies < 10ms performance
- test_get_callers_large_graph verifies < 100ms performance
- test_threshold_enforcement verifies 100 caller limit
- test_circular_dependency_detection verifies cycle_detected flag
- test_session_caching documents caching contract
- All tests pass with acceptable performance bounds
</success_criteria>

<output>
After completion, create `.planning/phases/12-rich-span-advanced/12-06-SUMMARY.md`
</output>
