---
phase: 12-rich-span-advanced
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hints/mod.rs
  - src/lib.rs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User receives requires_full_context boolean based on symbol analysis"
    - "User receives apply_atomically boolean (always true)"
    - "User receives may_break_tests boolean based on static heuristic"
    - "User receives requires_compilation boolean based on operation type"
    - "Tool hints derived from semantic_kind and symbol visibility"
  artifacts:
    - path: "src/hints/mod.rs"
      provides: "Tool hints derivation module"
      min_lines: 150
      exports: ["ToolHints", "derive_tool_hints"]
  key_links:
    - from: "src/hints/mod.rs"
      to: "src/ingest/semantic_kind.rs"
      via: "SemanticKind enum for hint derivation"
      pattern: "SemanticKind::"
    - from: "src/hints/mod.rs"
      to: "src/resolve/mod.rs"
      via: "ResolvedSpan for symbol metadata"
      pattern: "ResolvedSpan"
---

<objective>
Implement tool hints module with behavioral flags (requires_full_context, apply_atomically, may_break_tests, requires_compilation).

Purpose: Provide LLMs with guidance on safe operations and potential side effects of refactoring actions.
Output: ToolHints struct and derive_tool_hints function that analyzes symbol metadata to determine behavioral flags.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 11 summaries (semantic kind infrastructure)
@.planning/phases/11-rich-span-core/11-03-SUMMARY.md

# Codebase reference
@src/ingest/semantic_kind.rs
@src/resolve/mod.rs
@src/cli/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ToolHints struct with behavioral flags</name>
  <files>src/hints/mod.rs</files>
  <action>
    Create src/hints/mod.rs with ToolHints struct:

    1. ToolHints struct with fields:
       - requires_full_context: bool (closures/impl blocks capture outer scope)
       - apply_atomically: bool (always true for splice operations)
       - may_break_tests: bool (public functions, trait changes)
       - requires_compilation: bool (type-changing operations)

    Use serde::Serialize for JSON output. Apply #[serde(skip_serializing_if = "Option::is_none")] if fields become optional.

    Include builder methods: with_requires_full_context(), with_may_break_tests(), with_requires_compilation().
  </action>
  <verify>cargo check --lib passes</verify>
  <done>ToolHints struct with 4 boolean fields defined and serializable</done>
</task>

<task type="auto">
  <name>Task 2: Implement derive_tool_hints function</name>
  <files>src/hints/mod.rs</files>
  <action>
    Implement derive_tool_hints(signature, operation_type) -> ToolHints:

    1. requires_full_context logic:
       - True for: SemanticKind::Function if name contains "closure" or body is small (<5 lines)
       - True for: impl blocks (SemanticKind::Trait from impl_item)
       - False otherwise

    2. apply_atomically:
       - Always true (all splice operations are atomic)

    3. may_break_tests logic (static heuristic):
       - True for: public functions (check name visibility if available, or assume true)
       - True for: trait signatures and impl blocks
       - False for: private/internal symbols

    4. requires_compilation:
       - True for: type-changing operations (struct, enum, trait, type_alias modifications)
       - True for: public signature changes
       - False for: function body changes only

    Function signature:
    - derive_tool_hints(semantic_kind: SemanticKind, is_public: bool, operation: ToolHintOperation) -> ToolHints

    Where ToolHintOperation enum: DeleteBody, ChangeSignature, ChangeType, ReplaceBody.
  </action>
  <verify>cargo check --lib passes</verify>
  <done>derive_tool_hints returns appropriate flags based on symbol analysis</done>
</task>

<task type="auto">
  <name>Task 3: Add convenience constructors for common scenarios</name>
  <files>src/hints/mod.rs</files>
  <action>
    Add convenience constructors for common refactoring scenarios:

    1. ToolHints::for_function_delete(is_public: bool) -> Self
       - apply_atomically: true
       - may_break_tests: is_public
       - requires_compilation: true
       - requires_full_context: false

    2. ToolHints::for_struct_modify(is_public: bool) -> Self
       - apply_atomically: true
       - may_break_tests: is_public
       - requires_compilation: true
       - requires_full_context: false

    3. ToolHints::for_body_replace() -> Self
       - apply_atomically: true
       - may_break_tests: false
       - requires_compilation: false
       - requires_full_context: false

    These provide sensible defaults for common operations.
  </action>
  <verify>cargo check --lib passes</verify>
  <done>Convenience constructors available for common scenarios</done>
</task>

<task type="auto">
  <name>Task 4: Export hints module from lib.rs</name>
  <files>src/lib.rs</files>
  <action>
    Add "pub mod hints;" to src/lib.rs
    Add re-exports: "pub use hints::{ToolHints, ToolHintOperation, derive_tool_hints};"
  </action>
  <verify>cargo check --lib passes, cargo test --lib passes</verify>
  <done>hints module available at crate root</done>
</task>

</tasks>

<verification>
Overall phase checks:
- [ ] cargo check --lib passes with no warnings
- [ ] cargo test --lib passes all existing tests
- [ ] ToolHints struct has 4 boolean fields with serde Serialize
- [ ] derive_tool_hints accepts SemanticKind and operation type
- [ ] Convenience constructors cover 3 common scenarios
- [ ] apply_atomically always returns true
- [ ] Module exported from lib.rs
</verification>

<success_criteria>
Measurable completion:
- ToolHints struct with 4 fields (requires_full_context, apply_atomically, may_break_tests, requires_compilation)
- ToolHintOperation enum with 4 variants (DeleteBody, ChangeSignature, ChangeType, ReplaceBody)
- derive_tool_hints(kind: SemanticKind, is_public: bool, operation: ToolHintOperation) -> ToolHints
- 3 convenience constructors (for_function_delete, for_struct_modify, for_body_replace)
- Module exported as "hints" from lib.rs
</success_criteria>

<output>
After completion, create `.planning/phases/12-rich-span-advanced/12-02-SUMMARY.md`
</output>
