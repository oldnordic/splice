---
phase: 12-rich-span-advanced
plan: 05
type: execute
wave: 3
depends_on: [12-01, 12-02, 12-03, 12-04]
files_modified:
  - src/cli/mod.rs
  - src/main.rs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can use --relationships flag to enable relationship queries"
    - "Relationships only queried when --relationships flag is present (lazy)"
    - "CLI Commands (Delete, Patch) populate tool_hints in JSON output"
    - "CLI Commands populate suggested_action in JSON output"
    - "JSON output includes all 3 new fields when requested"
  artifacts:
    - path: "src/cli/mod.rs"
      provides: "CLI with --relationships flag"
      contains: "relationships: bool"
    - path: "src/main.rs"
      provides: "CLI integration for advanced fields"
      contains: "with_relationships"
  key_links:
    - from: "src/main.rs"
      to: "src/relationships/mod.rs"
      via: "Call get_callers/get_callees when flag present"
      pattern: "relationships::get_callers|relationships::get_callees"
    - from: "src/main.rs"
      to: "src/hints/mod.rs"
      via: "Call derive_tool_hints for operations"
      pattern: "hints::derive_tool_hints"
    - from: "src/main.rs"
      to: "src/action/mod.rs"
      via: "Call suggest_action for operations"
      pattern: "action::suggest_action"
---

<objective>
Integrate --relationships flag into CLI and wire up tool_hints and suggested_action for JSON output.

Purpose: Enable lazy relationship queries (only when --relationships flag is present) and populate all advanced span fields in CLI JSON output.
Output: CLI with --relationships flag and JSON output including relationships, tool_hints, suggested_action.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 11 summaries
@.planning/phases/11-rich-span-core/11-07-SUMMARY.md

# Codebase reference
@src/cli/mod.rs
@src/main.rs
@src/relationships/mod.rs (from 12-01)
@src/hints/mod.rs (from 12-02)
@src/action/mod.rs (from 12-03)
@src/output.rs (from 12-04)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --relationships flag to CLI Commands</name>
  <files>src/cli/mod.rs</files>
  <action>
    Add --relationships flag to Commands that support rich span output:

    1. Delete command:
       - Add #[arg(long)] relationships: bool
       - Default: false

    2. Patch command:
       - Add #[arg(long)] relationships: bool
       - Default: false

    3. Query command:
       - Add #[arg(long)] relationships: bool
       - Default: false

    4. Get command:
       - Add #[arg(long)] relationships: bool
       - Default: false

    Follow existing flag pattern (similar to context_lines, verbose).
  </action>
  <verify>cargo check passes, splice --help shows new flag</verify>
  <done>--relationships flag available on Delete, Patch, Query, Get commands</done>
</task>

<task type="auto">
  <name>Task 2: Wire up relationship queries in main.rs for Delete command</name>
  <files>src/main.rs</files>
<action>
  In execute_delete() function in main.rs:

  1. After resolving the symbol, check if cli.relationships is true
  2. If true, call relationship functions:
     - get_callers(graph, resolved_symbol.node_id)
     - get_callees(graph, resolved_symbol.node_id)
     - get_imports(graph, file_path)
     - get_exports(graph, file_path)
  3. Create Relationships struct and attach via span_result.with_relationships()

  Import the relationships module at top of main.rs:
  - use splice::relationships::{get_callers, get_callees, get_imports, get_exports, Relationships};

  Only query when flag is present (lazy evaluation).
</action>
<verify>cargo check passes</verify>
<done>Delete command queries relationships when --relationships flag present</done>
</task>

<task type="auto">
  <name>Task 3: Wire up tool_hints and suggested_action in main.rs</name>
  <files>src/main.rs</files>
  <action>
  In execute_delete() and execute_single_patch() functions:

  1. Derive tool_hints:
     - Call derive_tool_hints(semantic_kind, is_public, operation_type)
     - Attach via span_result.with_tool_hints(hints)

  2. Generate suggested_action:
     - Call suggest_action(action_type, symbol_name, symbol_kind, file_path, confidence)
     - Attach via span_result.with_suggested_action(action)

  For execute_delete():
  - action_type = ActionType::Delete
  - operation = ToolHintOperation::DeleteBody
  - is_public = infer from symbol kind (functions, structs default to true)

  For execute_single_patch():
  - action_type = ActionType::Replace
  - operation = ToolHintOperation::ReplaceBody
  - is_public = infer from symbol kind

  Import modules:
  - use splice::hints::{derive_tool_hints, ToolHintOperation};
  - use splice::action::{suggest_action, ActionType, Confidence};
  - use splice::ingest::semantic_kind::SemanticKind;
</action>
<verify>cargo check passes</verify>
<done>tool_hints and suggested_action populated in JSON output</done>
</task>

<task type="auto">
  <name>Task 4: Run integration tests for CLI output</name>
  <files>src/main.rs</files>
  <action>
  Run cargo test --test integration to verify:
  1. Delete with --relationships includes relationships in JSON output
  2. Delete without --relationships omits relationships field
  3. Delete output includes tool_hints with appropriate flags
  4. Delete output includes suggested_action with Delete type
  5. Patch output includes suggested_action with Replace type
  6. All existing CLI tests still pass

  Run manual test:
  - cargo run -- delete --file test.rs --symbol foo --json
  - Verify JSON output includes tool_hints and suggested_action
  - Verify JSON output does NOT include relationships (flag not present)
</action>
<verify>cargo test --test integration passes, manual CLI test produces expected JSON</verify>
<done>CLI integration verified with relationships, tool_hints, suggested_action in output</done>
</task>

</tasks>

<verification>
Overall phase checks:
- [ ] cargo check passes with no warnings
- [ ] cargo test --lib passes all tests
- [ ] --relationships flag available on Delete, Patch, Query, Get commands
- [ ] Relationships only queried when --relationships flag is present
- [ ] tool_hints populated in all relevant operations
- [ ] suggested_action populated in all relevant operations
- [ ] JSON output includes all 3 fields when applicable
- [ ] JSON output omits fields when None/not requested
- [ ] All existing CLI tests pass
</verification>

<success_criteria>
Measurable completion:
- --relationships flag on Delete, Patch, Query, Get commands
- execute_delete() queries relationships when flag present
- execute_delete() populates tool_hints via derive_tool_hints()
- execute_delete() populates suggested_action via suggest_action()
- execute_single_patch() populates tool_hints and suggested_action
- JSON output includes relationships: {} when --relationships flag present
- JSON output omits relationships when flag absent
- JSON output always includes tool_hints and suggested_action for operations
</success_criteria>

<output>
After completion, create `.planning/phases/12-rich-span-advanced/12-05-SUMMARY.md`
</output>
