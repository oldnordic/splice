---
phase: 12-rich-span-advanced
plan: 05
type: execute
wave: 3
depends_on: [12-01, 12-02, 12-03, 12-04]
files_modified:
  - src/cli/mod.rs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can use --relationships flag on Delete command"
    - "User can use --relationships flag on Patch command"
    - "User can use --relationships flag on Query command"
    - "User can use --relationships flag on Get command"
    - "All flags default to false (lazy evaluation)"
  artifacts:
    - path: "src/cli/mod.rs"
      provides: "CLI with --relationships flag"
      contains: "relationships: bool"
  key_links:
    - from: "src/cli/mod.rs"
      to: "src/main.rs"
      via: "Command struct passes relationships flag to execute functions"
      pattern: "relationships.*bool"
---

<objective>
Add --relationships flag to CLI Commands that support rich span output.

Purpose: Enable lazy relationship queries (only when --relationships flag is present) for all relevant CLI commands.
Output: CLI Commands with --relationships flag available.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Codebase reference
@src/cli/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --relationships flag to CLI Commands</name>
  <files>src/cli/mod.rs</files>
  <action>
    Add --relationships flag to Commands that support rich span output:

    1. Delete command:
       - Add #[arg(long)] relationships: bool
       - Default: false

    2. Patch command:
       - Add #[arg(long)] relationships: bool
       - Default: false

    3. Query command:
       - Add #[arg(long)] relationships: bool
       - Default: false

    4. Get command:
       - Add #[arg(long)] relationships: bool
       - Default: false

    Follow existing flag pattern (similar to context_lines, verbose).
  </action>
  <verify>cargo check passes, splice --help shows new flag for each command</verify>
  <done>--relationships flag available on Delete, Patch, Query, Get commands</done>
</task>

</tasks>

<verification>
Overall phase checks:
- [ ] cargo check passes with no warnings
- [ ] --help shows --relationships flag for Delete command
- [ ] --help shows --relationships flag for Patch command
- [ ] --help shows --relationships flag for Query command
- [ ] --help shows --relationships flag for Get command
- [ ] All flags default to false
</verification>

<success_criteria>
Measurable completion:
- --relationships flag on Delete command (bool, default false)
- --relationships flag on Patch command (bool, default false)
- --relationships flag on Query command (bool, default false)
- --relationships flag on Get command (bool, default false)
- All flags follow existing CLI pattern
</success_criteria>

<output>
After completion, create `.planning/phases/12-rich-span-advanced/12-05-SUMMARY.md`
</output>
