---
phase: 12-rich-span-advanced
plan: 08
type: execute
wave: 4
depends_on: [12-01, 12-02, 12-03, 12-04, 12-05]
files_modified:
  - src/main.rs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Query command populates tool_hints and suggested_action in JSON output"
    - "Get command populates tool_hints and suggested_action in JSON output"
    - "Delete command populates tool_hints and suggested_action in JSON output"
    - "Patch command populates tool_hints and suggested_action in JSON output"
    - "tool_hints and suggested_action values vary appropriately by command type"
  artifacts:
    - path: "src/main.rs"
      provides: "CLI integration for tool_hints and suggested_action in all commands"
      contains: "execute_query|execute_get|execute_delete|execute_single_patch"
  key_links:
    - from: "src/main.rs"
      to: "src/hints/mod.rs"
      via: "Call derive_tool_hints for all operations"
      pattern: "hints::derive_tool_hints"
    - from: "src/main.rs"
      to: "src/action/mod.rs"
      via: "Call suggest_action for all operations"
      pattern: "action::suggest_action"
---

<objective>
Wire up tool_hints and suggested_action fields for all CLI commands (Query, Get, Delete, Patch).

Purpose: Enable tool hints (flags for LLM consumption) and suggested actions across all command types.
Output: All four commands populate tool_hints and suggested_action in JSON output.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 11 summaries
@.planning/phases/11-rich-span-core/11-07-SUMMARY.md

# Codebase reference
@src/main.rs
@src/hints/mod.rs (from 12-02)
@src/action/mod.rs (from 12-03)
@src/output.rs (from 12-04)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire up tool_hints and suggested_action for Query and Get commands</name>
  <files>src/main.rs</files>
  <action>
    In execute_query() and execute_get() functions:

    1. Derive tool_hints:
       - Call derive_tool_hints(semantic_kind, is_public, operation)
       - operation = ToolHintOperation::Query for execute_query()
       - operation = ToolHintOperation::Get for execute_get()
       - Attach via span_result.with_tool_hints(hints)

    2. Generate suggested_action:
       - Call suggest_action(action_type, symbol_name, symbol_kind, file_path, confidence)
       - action_type = ActionType::Query for execute_query()
       - action_type = ActionType::Read for execute_get()
       - confidence = Confidence::High (symbol resolved)
       - Attach via span_result.with_suggested_action(action)

    Import modules:
    - use splice::hints::{derive_tool_hints, ToolHintOperation};
    - use splice::action::{suggest_action, ActionType, Confidence};
    - use splice::ingest::semantic_kind::SemanticKind;

    Infer is_public from symbol kind (functions, structs, enums default to true).
  </action>
  <verify>cargo check passes</verify>
  <done>tool_hints and suggested_action populated in Query and Get command output</done>
</task>

<task type="auto">
  <name>Task 2: Wire up tool_hints and suggested_action for Delete command</name>
  <files>src/main.rs</files>
  <action>
    In execute_delete() function:

    1. Derive tool_hints:
       - Call derive_tool_hints(semantic_kind, is_public, ToolHintOperation::DeleteBody)
       - Attach via span_result.with_tool_hints(hints)
       - This sets may_break_tests based on caller count

    2. Generate suggested_action:
       - Call suggest_action(ActionType::Delete, symbol_name, symbol_kind, file_path, confidence)
       - confidence = Confidence::High if no callers, Medium if callers exist
       - reason should reflect safety: "safe to delete - no callers" or "has callers - may break"
       - Attach via span_result.with_suggested_action(action)

    For Delete:
    - action_type = ActionType::Delete
    - operation = ToolHintOperation::DeleteBody
    - is_public = infer from symbol kind
  </action>
  <verify>cargo check passes</verify>
  <done>tool_hints and suggested_action populated in Delete command output</done>
</task>

<task type="auto">
  <name>Task 3: Wire up tool_hints and suggested_action for Patch command and verify JSON output</name>
  <files>src/main.rs</files>
  <action>
    In execute_single_patch() function:

    1. Derive tool_hints:
       - Call derive_tool_hints(semantic_kind, is_public, ToolHintOperation::ReplaceBody)
       - Attach via span_result.with_tool_hints(hints)

    2. Generate suggested_action:
       - Call suggest_action(ActionType::Replace, symbol_name, symbol_kind, file_path, confidence)
       - confidence = Confidence::High for unique symbols, Medium for ambiguous
       - Attach via span_result.with_suggested_action(action)

    3. Run integration tests to verify:
       - All commands include tool_hints with appropriate flags
       - All commands include suggested_action with correct action_type
       - JSON output structure is correct:
         ```json
         {
           "tool_hints": {
             "requires_full_context": false,
             "may_break_tests": false,
             "requires_compilation": false,
             "apply_atomically": true
           },
           "suggested_action": {
             "action_type": "query|read|delete|replace",
             "confidence": "high|medium",
             "reason": "explanation"
           }
         }
         ```

    4. Manual verification:
       - cargo run -- query --file test.rs --symbol foo --json
       - cargo run -- get --file test.rs --symbol bar --json
       - cargo run -- delete --file test.rs --symbol baz --json
       - cargo run -- patch --file test.rs --symbol qux --json
  </action>
  <verify>cargo test --test integration passes, manual CLI test produces expected JSON structure with exact field names</verify>
  <done>All commands verified with tool_hints and suggested_action in output with correct JSON structure</done>
</task>

</tasks>

<verification>
Overall phase checks:
- [ ] cargo check passes with no warnings
- [ ] cargo test --lib passes all tests
- [ ] Query command populates tool_hints via derive_tool_hints()
- [ ] Query command populates suggested_action with ActionType::Query
- [ ] Get command populates tool_hints and suggested_action
- [ ] Delete command populates tool_hints with may_break_tests set appropriately
- [ ] Delete command populates suggested_action with ActionType::Delete
- [ ] Patch command populates tool_hints and suggested_action
- [ ] JSON output includes tool_hints: {requires_full_context, may_break_tests, requires_compilation, apply_atomically}
- [ ] JSON output includes suggested_action: {action_type, confidence, reason}
- [ ] All existing CLI tests pass
</verification>

<success_criteria>
Measurable completion:
- execute_query() populates tool_hints with ToolHintOperation::Query
- execute_query() populates suggested_action with ActionType::Query
- execute_get() populates tool_hints with ToolHintOperation::Get
- execute_get() populates suggested_action with ActionType::Read
- execute_delete() populates tool_hints with ToolHintOperation::DeleteBody
- execute_delete() populates suggested_action with ActionType::Delete
- execute_single_patch() populates tool_hints with ToolHintOperation::ReplaceBody
- execute_single_patch() populates suggested_action with ActionType::Replace
- JSON output has exact structure for tool_hints and suggested_action fields
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-rich-span-advanced/12-08-SUMMARY.md`
</output>
