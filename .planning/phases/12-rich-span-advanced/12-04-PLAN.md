---
phase: 12-rich-span-advanced
plan: 04
type: execute
wave: 2
depends_on: [12-01, 12-02, 12-03]
files_modified:
  - src/output.rs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "SpanResult includes relationships field when populated"
    - "SpanResult includes tool_hints field when populated"
    - "SpanResult includes suggested_action field when populated"
    - "All new fields are optional with skip_serializing_if"
    - "Builder methods available for each new field"
  artifacts:
    - path: "src/output.rs"
      provides: "Extended SpanResult with advanced fields"
      contains: "relationships: Option<Relationships>"
      contains: "tool_hints: Option<ToolHints>"
      contains: "suggested_action: Option<SuggestedAction>"
  key_links:
    - from: "src/output.rs"
      to: "src/relationships/mod.rs"
      via: "Use Relationships type from relationships module"
      pattern: "use crate::relationships::Relationships"
    - from: "src/output.rs"
      to: "src/hints/mod.rs"
      via: "Use ToolHints type from hints module"
      pattern: "use crate::hints::ToolHints"
    - from: "src/output.rs"
      to: "src/action/mod.rs"
      via: "Use SuggestedAction type from action module"
      pattern: "use crate::action::SuggestedAction"
---

<objective>
Extend SpanResult with advanced fields (relationships, tool_hints, suggested_action) using additive-only schema.

Purpose: Enable rich span output with relationships, behavioral guidance, and action suggestions for LLM consumption.
Output: SpanResult with 3 new optional fields and corresponding builder methods.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 11 summaries (SpanResult foundation)
@.planning/phases/11-rich-span-core/11-01-SUMMARY.md

# Codebase reference
@src/output.rs
@src/relationships/mod.rs (from 12-01)
@src/hints/mod.rs (from 12-02)
@src/action/mod.rs (from 12-03)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add imports for new types to output.rs</name>
  <files>src/output.rs</files>
  <action>
    Add imports to src/output.rs for the new types:
    - use crate::relationships::Relationships;
    - use crate::hints::ToolHints;
    - use crate::action::SuggestedAction;

    Place imports after existing imports, before the Schema version constant.
  </action>
  <verify>cargo check --lib passes</verify>
  <done>New types imported and available in output module</done>
</task>

<task type="auto">
  <name>Task 2: Add relationships, tool_hints, suggested_action fields to SpanResult</name>
  <files>src/output.rs</files>
  <action>
    Add 3 new optional fields to SpanResult struct (after existing rich span fields):

    1. relationships: Option<Relationships>
    2. tool_hints: Option<ToolHints>
    3. suggested_action: Option<SuggestedAction>

    Apply #[serde(skip_serializing_if = "Option::is_none")] to each.

    Update from_byte_span() to initialize these as None.
    Update from<crate::patch::FilePatchSummary>() to initialize these as None.
    Update from<crate::resolve::ResolvedSpan>() to initialize these as None.
  </action>
  <verify>cargo check --lib passes, all 334+ existing tests pass</verify>
  <done>SpanResult extended with 3 new optional fields</done>
</task>

<task type="auto">
  <name>Task 3: Add builder methods for new fields</name>
  <files>src/output.rs</files>
  <action>
    Add builder methods to impl SpanResult (after existing builder methods):

    1. with_relationships(mut self, relationships: Relationships) -> Self
    2. with_tool_hints(mut self, hints: ToolHints) -> Self
    3. with_suggested_action(mut self, action: SuggestedAction) -> Self

    Follow existing builder pattern (e.g., with_context(), with_semantic_kind()).
  </action>
  <verify>cargo check --lib passes</verify>
  <done>Builder methods available for all 3 new fields</done>
</task>

<task type="auto">
  <name>Task 4: Run all tests to verify backward compatibility</name>
  <files>src/output.rs</files>
  <action>
    Run cargo test --lib to verify:
    1. All existing tests pass (additive schema only)
    2. No breaking changes to JSON output
    3. New fields serialize correctly when populated
    4. New fields are omitted when None

    Run cargo test --doc to verify documentation tests pass.
  </action>
  <verify>cargo test --lib passes all tests</verify>
  <done>Backward compatibility verified with all existing tests passing</done>
</task>

</tasks>

<verification>
Overall phase checks:
- [ ] cargo check --lib passes with no warnings
- [ ] cargo test --lib passes all 334+ existing tests
- [ ] SpanResult has 3 new optional fields (relationships, tool_hints, suggested_action)
- [ ] All new fields use skip_serializing_if for backward compatibility
- [ ] Builder methods exist for all 3 new fields
- [ ] from_byte_span() initializes new fields to None
- [ ] JSON output omits new fields when None
</verification>

<success_criteria>
Measurable completion:
- SpanResult with relationships: Option<Relationships> field
- SpanResult with tool_hints: Option<ToolHints> field
- SpanResult with suggested_action: Option<SuggestedAction> field
- with_relationships() builder method
- with_tool_hints() builder method
- with_suggested_action() builder method
- All existing 334+ tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-rich-span-advanced/12-04-SUMMARY.md`
</output>
