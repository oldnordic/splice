---
phase: 12-rich-span-advanced
plan: 03
type: execute
wave: 2
depends_on: [12-02]
files_modified:
  - src/action/mod.rs
  - src/lib.rs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User receives action_type (delete, replace, expand) based on operation"
    - "User receives confidence level (High/Medium/Low) based on uniqueness"
    - "User receives reason explaining why this action was suggested"
    - "Action type matches CLI command (delete -> delete, patch -> replace, expand -> expand)"
  artifacts:
    - path: "src/action/mod.rs"
      provides: "Suggested action module"
      min_lines: 150
      exports: ["SuggestedAction", "ActionType", "Confidence"]
  key_links:
    - from: "src/action/mod.rs"
      to: "src/resolve/mod.rs"
      via: "ResolvedSpan for symbol uniqueness"
      pattern: "ResolvedSpan"
    - from: "src/action/mod.rs"
      to: "src/cli/mod.rs"
      via: "Commands enum for action type mapping"
      pattern: "Commands::"
---

<objective>
Implement suggested action engine with action types (delete, replace, expand), confidence levels, and reasoning.

Purpose: Guide LLMs on recommended actions with confidence assessment based on symbol uniqueness and ambiguity.
Output: SuggestedAction struct with action_type, confidence, reason, and optional params.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 11 summaries
@.planning/phases/11-rich-span-core/11-01-SUMMARY.md
@.planning/phases/11-rich-span-core/11-03-SUMMARY.md

# Codebase reference
@src/resolve/mod.rs
@src/cli/mod.rs
@src/error_codes.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ActionType, Confidence, and SuggestedAction structs</name>
  <files>src/action/mod.rs</files>
  <action>
    Create src/action/mod.rs with:

    1. ActionType enum (3 variants):
       - Delete (for delete operations)
       - Replace (for patch operations)
       - Expand (for expand operations)
       Use serde(rename_all = "lowercase") for JSON output (delete, replace, expand)

    2. Confidence enum (3 variants):
       - High (unique symbol, no ambiguity)
       - Medium (some ambiguity or multiple matches)
       - Low (highly ambiguous, missing type info)
       Use serde(rename_all = "lowercase") for JSON output

    3. SuggestedAction struct:
       - action_type: ActionType
       - confidence: Confidence
       - reason: String (human-readable explanation)
       - params: Option<HashMap<String, Value>> (additional action parameters)

    Apply #[serde(skip_serializing_if = "Option::is_none")] to params field.
  </action>
  <verify>cargo check --lib passes</verify>
  <done>ActionType, Confidence, and SuggestedAction structs defined and serializable</done>
</task>

<task type="auto">
  <name>Task 2: Implement confidence calculation logic</name>
  <files>src/action/mod.rs</files>
  <action>
    Implement Confidence::from_resolution(resolution_result) -> Confidence:

    1. High confidence when:
       - Symbol is uniquely resolved (single match)
       - Has explicit file path and kind
       - Has type information available

    2. Medium confidence when:
       - Multiple matches exist but file is specified
       - Kind is specified but ambiguous
       - Some type info available

    3. Low confidence when:
       - Name-only resolution with multiple candidates
       - Missing file path
       - Missing kind/type information
       - AmbiguousSymbol error was encountered

    Function signature:
    - Confidence::calculate(is_unique: bool, has_file: bool, has_kind: bool, is_ambiguous: bool) -> Self
  </action>
  <verify>cargo check --lib passes</verify>
  <done>Confidence calculation based on resolution uniqueness and metadata</done>
</task>

<task type="auto">
  <name>Task 3: Implement suggest_action function with reasoning</name>
  <files>src/action/mod.rs</files>
  <action>
    Implement suggest_action(command, symbol_info) -> SuggestedAction:

    1. Map CLI Commands to ActionType:
       - Commands::Delete -> ActionType::Delete
       - Commands::Patch -> ActionType::Replace
       - Commands::Query/Get -> ActionType::Expand (for context expansion)

    2. Generate reason based on:
       - Delete: "Delete symbol '{name}' ({kind}) at {file_path}:{line}"
       - Replace: "Replace symbol '{name}' with provided content"
       - Expand: "Expand symbol to include full context ({level} levels)"

    3. Include params when relevant:
       - For Expand: include "levels": 2 for name->body->block
       - For Replace: include "preserve_signature": true
       - For Delete: include "remove_references": true

    Function signature:
    - suggest_action(action_type: ActionType, symbol_name: &str, symbol_kind: &str, file_path: &str, confidence: Confidence) -> SuggestedAction
  </action>
  <verify>cargo check --lib passes</verify>
  <done>suggest_action returns SuggestedAction with type, confidence, and reason</done>
</task>

<task type="auto">
  <name>Task 4: Export action module from lib.rs</name>
  <files>src/lib.rs</files>
  <action>
    Add "pub mod action;" to src/lib.rs
    Add re-exports: "pub use action::{SuggestedAction, ActionType, Confidence, suggest_action};"
  </action>
  <verify>cargo check --lib passes, cargo test --lib passes</verify>
  <done>action module available at crate root</done>
</task>

</tasks>

<verification>
Overall phase checks:
- [ ] cargo check --lib passes with no warnings
- [ ] cargo test --lib passes all existing tests
- [ ] ActionType enum has 3 variants (Delete, Replace, Expand)
- [ ] Confidence enum has 3 variants (High, Medium, Low)
- [ ] SuggestedAction struct has 4 fields (action_type, confidence, reason, params)
- [ ] Confidence calculation uses uniqueness and metadata
- [ ] suggest_action generates human-readable reasons
- [ ] Module exported from lib.rs
</verification>

<success_criteria>
Measurable completion:
- ActionType enum with Delete, Replace, Expand variants
- Confidence enum with High, Medium, Low variants
- SuggestedAction struct with action_type, confidence, reason, params
- Confidence::calculate(is_unique, has_file, has_kind, is_ambiguous) function
- suggest_action(action_type, symbol_name, symbol_kind, file_path, confidence) function
- Module exported as "action" from lib.rs
</success_criteria>

<output>
After completion, create `.planning/phases/12-rich-span-advanced/12-03-SUMMARY.md`
</output>
