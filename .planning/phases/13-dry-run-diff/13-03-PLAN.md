---
phase: 13-dry-run-diff
plan: 03
type: execute
wave: 2
depends_on: [13-01, 13-02]
files_modified:
  - src/cli/mod.rs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can run `splice patch --dry-run` as alias for `--preview`"
    - "User can run `splice patch -n` as short form of `--dry-run`"
    - "User can run `splice delete --dry-run` for previewing deletions"
    - "User can specify `--unified <n>` to control context lines (default 3)"
    - "CLI parsing respects flag conflicts (dry-run conflicts with batch)"
  artifacts:
    - path: "src/cli/mod.rs"
      provides: "CLI flag definitions for dry-run and unified context"
      contains: "dry_run"
      contains: "unified"
  key_links:
    - from: "src/cli/mod.rs"
      to: "src/diff/mod.rs"
      via: "unified parameter passed through Commands enum"
      pattern: "unified.*usize"
---

<objective>
Add CLI flags for dry-run mode and unified diff context configuration.

Purpose: Standard CLI tools use `--dry-run` or `-n` for preview mode (like `make -n`, `rsync --dry-run`, `kubectl apply --dry-run`). The existing `--preview` flag should gain these aliases for familiarity. Additionally, the `--unified <n>` flag allows users to control how many context lines appear in the diff (following `git diff -U<n>` convention).

Output: Updated CLI with dry-run aliases and configurable unified context lines.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-dry-run-diff/13-RESEARCH.md
@src/cli/mod.rs
@.planning/phases/13-dry-run-diff/13-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dry-run alias to Patch command</name>
  <files>src/cli/mod.rs</files>
  <action>
    Modify the Patch command in src/cli/mod.rs (around lines 84-141):

    1. Change the existing `preview: bool` field to add short and long aliases:
       - Replace: `#[arg(long, conflicts_with = "batch")]`
       - With: `#[arg(short = 'n', long = "dry-run", alias = "preview", conflicts_with = "batch")]`
       - Keep the field name as `preview` (to avoid breaking existing code)
       - This enables: `-n`, `--dry-run`, and `--preview` all to work

    2. Update the doc comment from "Run in preview mode" to "Preview changes without applying (alias: --dry-run, -n)"

    3. Add the new `unified` field after `preview`:
       - Add: `/// Number of context lines in unified diff (default: 3).`
       - Add: `#[arg(short = 'U', long, value_name = "N", default_value = "3")]`
       - Add: `unified: usize,`

    4. Update the Patch command struct to include `unified` in the pattern match in main.rs (this will be done in a later plan)

    DO NOT modify batch handling (dry-run continues to conflict with batch)
  </action>
  <verify>cargo check</verify>
  <done>Patch command accepts -n/--dry-run/--preview aliases and --unified flag</done>
</task>

<task type="auto">
  <name>Task 2: Add dry-run flag to Delete command</name>
  <files>src/cli/mod.rs</files>
  <action>
    Modify the Delete command in src/cli/mod.rs (around lines 41-81) to add dry-run support:

    1. Add the `dry_run` field after `relationships` (around line 72):
       - Add: `/// Preview deletion without applying changes.`
       - Add: `#[arg(short = 'n', long = "dry-run")]`
       - Add: `dry_run: bool,`

    2. Add the `unified` field after `dry_run`:
       - Add: `/// Number of context lines in unified diff (default: 3).`
       - Add: `#[arg(short = 'U', long, value_name = "N", default_value = "3")]`
       - Add: `unified: usize,`

    The Delete command doesn't have batch mode, so no conflicts_with is needed.

    This enables consistent preview behavior across both patch and delete commands.
  </action>
  <verify>cargo check</verify>
  <done>Delete command accepts --dry-run/-n flag and --unified flag</done>
</task>

</tasks>

<verification>
- [ ] `cargo check` passes with new CLI flags
- [ ] `splice patch --help` shows `-n, --dry-run` and `--unified <N>` options
- [ ] `splice delete --help` shows `--dry-run` and `--unified <N>` options
- [ ] Existing `--preview` flag still works (backward compatibility)
</verification>

<success_criteria>
Both Patch and Delete commands support dry-run with configurable unified context lines.
</success_criteria>

<output>
After completion, create `.planning/phases/13-dry-run-diff/13-03-SUMMARY.md`
</output>
