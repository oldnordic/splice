---
phase: 13-dry-run-diff
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Dependencies compile successfully with `cargo check`"
    - "`similar` crate provides `TextDiff::unified_diff()` for unified diff generation"
    - "`nu-ansi-term` crate provides `Color` enum for terminal colors"
    - "`is-terminal` crate provides `IsTerminal` trait for TTY detection"
  artifacts:
    - path: "Cargo.toml"
      provides: "Dependency declarations for similar, nu-ansi-term, is-terminal"
      contains: "similar = "
    - path: "Cargo.lock"
      provides: "Locked versions for new dependencies"
  key_links:
    - from: "Cargo.toml"
      to: "crates.io"
      via: "cargo build"
      pattern: "similar.*nu_ansi_term.*is_terminal"
---

<objective>
Add diff-related dependencies to enable unified diff generation with colored terminal output.

Purpose: The `similar` crate provides unified diff generation (de facto standard in Rust), `nu-ansi-term` is actively maintained by Nushell for terminal colors (successor to deprecated `ansi_term`), and `is-terminal` provides cross-platform TTY detection (successor to unmaintained `atty`).

Output: Updated Cargo.toml with three new dependencies that compile successfully.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-dry-run-diff/13-RESEARCH.md
@Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add similar, nu-ansi-term, and is-terminal dependencies</name>
  <files>Cargo.toml</files>
  <action>
    Add three dependencies to the [dependencies] section in Cargo.toml:

    1. `similar = "2.6"` - For unified diff generation using TextDiff::unified_diff()
    2. `nu-ansi-term = "0.50"` - For terminal colors (red for deletions, green for additions)
    3. `is-terminal = "0.4"` - For cross-platform TTY detection

    Place these in the [dependencies] section after the existing dependencies. Group them together with a comment "# Diff output" for clarity.

    Do NOT enable any optional features - the default features provide all needed functionality:
    - similar: default includes "text" feature for unified diff
    - nu-ansi-term: default includes all color support
    - is-terminal: no features needed

    Format: Follow the existing pattern in Cargo.toml (name, version, inline comment)
  </action>
  <verify>cargo check</verify>
  <done>Dependencies added, cargo check passes, Cargo.lock updated with new crates</done>
</task>

</tasks>

<verification>
- [ ] `cargo check` completes successfully
- [ ] `cargo build` completes successfully
- [ ] Cargo.lock contains entries for similar, nu-ansi-term, is-terminal
- [ ] No dependency conflicts or warnings
</verification>

<success_criteria>
All three dependencies compile without errors or conflicts.
</success_criteria>

<output>
After completion, create `.planning/phases/13-dry-run-diff/13-01-SUMMARY.md`
</output>
