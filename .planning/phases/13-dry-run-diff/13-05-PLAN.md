---
phase: 13-dry-run-diff
plan: 05
type: execute
wave: 4
depends_on: [13-04]
files_modified:
  - src/main.rs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Dry-run returns exit code 0 when no changes would be made"
    - "Dry-run returns exit code 1 when changes would be made"
    - "Normal operation returns exit code 0 on success, 1 on error"
    - "Exit code behavior matches `git diff --exit-code` convention"
    - "Pre-commit hooks can detect pending changes via exit code"
  artifacts:
    - path: "src/main.rs"
      provides: "Exit code handling for dry-run mode"
      contains: "ExitCode::from(1)"
  key_links:
    - from: "src/main.rs"
      to: "process::ExitCode"
      via: "ExitCode::from(1) for pending changes"
      pattern: "ExitCode.*preview.*has_changes"
---

<objective>
Implement git-style exit codes for dry-run mode (0=no changes, 1=changes pending).

Purpose: Following the `git diff --exit-code` convention, dry-run mode should return exit code 1 if changes would be made, and 0 if no changes. This enables pre-commit hooks and scripts to detect pending changes programmatically. Normal (non-dry-run) operations continue to use the existing exit code pattern (0=success, 1=error).

Output: Dry-run mode returns meaningful exit codes for scripting/integration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-dry-run-diff/13-RESEARCH.md
@src/main.rs
@src/cli/mod.rs
@src/patch/mod.rs
@.planning/phases/13-dry-run-diff/13-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement dry-run exit code logic in Patch command</name>
  <files>src/main.rs</files>
  <action>
    Modify the Patch command exit code handling in src/main.rs:

    1. Locate the execute_single_patch function result handling

    2. When preview/dry_run is true:
       - Check if changes would be made by inspecting the PreviewReport:
         - If `lines_added == 0 && lines_removed == 0`: return ExitCode::SUCCESS (0)
         - Otherwise: return ExitCode::from(1) (changes pending)
       - This follows the `git diff --exit-code` pattern

    3. When preview/dry_run is false (normal operation):
       - Keep existing behavior: ExitCode::SUCCESS on success, error code on failure

    4. The pattern should be:
       ```rust
       let exit_code = if preview {
           if report.lines_added > 0 || report.lines_removed > 0 {
               ExitCode::from(1)  // Changes pending (git diff convention)
           } else {
               ExitCode::SUCCESS   // No changes
           }
       } else {
           match result {
               Ok(_) => ExitCode::SUCCESS,
               Err(_) => ExitCode::from(1),
           }
       };
       ```

    5. Update the main function to return this exit_code instead of the hardcoded SUCCESS

    DO NOT change error handling exit codes - only dry-run mode uses the inverted convention.
  </action>
  <verify>cargo check</verify>
  <done>Patch command dry-run returns exit code 1 for changes, 0 for no changes</done>
</task>

<task type="auto">
  <name>Task 2: Implement dry-run exit code logic in Delete command</name>
  <files>src/main.rs</files>
  <action>
    Modify the Delete command exit code handling in src/main.rs:

    1. Locate the execute_delete function result handling

    2. When dry_run is true:
       - Check if the symbol was found and would be deleted
       - If symbol not found: return ExitCode::SUCCESS (0, nothing to delete)
       - If symbol found: return ExitCode::from(1) (changes pending)

    3. When dry_run is false (normal operation):
       - Keep existing behavior: ExitCode::SUCCESS on success, error code on failure

    4. The pattern mirrors Patch:
       ```rust
       let exit_code = if dry_run {
           if symbol_found && span_valid {
               ExitCode::from(1)  // Changes pending
           } else {
               ExitCode::SUCCESS   // No changes (symbol not found)
           }
       } else {
           match result {
               Ok(_) => ExitCode::SUCCESS,
               Err(_) => ExitCode::from(1),
           }
       };
       ```

    5. Handle the error case where symbol is not found consistently:
       - In dry-run mode: return 0 (no changes to make)
       - In normal mode: return error exit code (operation failed)
  </action>
  <verify>cargo check</verify>
  <done>Delete command dry-run returns exit code 1 for changes, 0 for no changes</done>
</task>

<task type="auto">
  <name>Task 3: Add integration tests for exit code behavior</name>
  <files>tests/cli_dry_run.rs</files>
  <action>
    Create a new test file tests/cli_dry_run.rs with integration tests for exit code behavior:

    Tests should cover:
    1. test_patch_dry_run_no_changes() - Patch with identical content returns exit code 0
    2. test_patch_dry_run_with_changes() - Patch with actual changes returns exit code 1
    3. test_patch_normal_success() - Normal patch success returns exit code 0
    4. test_patch_normal_failure() - Normal patch failure returns exit code 1
    5. test_delete_dry_run_symbol_not_found() - Delete non-existent symbol returns exit code 0
    6. test_delete_dry_run_symbol_found() - Delete existing symbol returns exit code 1

    Use std::process::Command to run splice CLI with various flags and assert on exit codes.

    Create temporary test files with known content to ensure deterministic behavior.

    Mark tests as #[ignore] if they require a full workspace setup, or use --test-threads=1.
  </action>
  <verify>cargo test --test cli_dry_run</verify>
  <done>Integration tests verify exit code behavior matches git diff convention</done>
</task>

</tasks>

<verification>
- [ ] `cargo check` passes
- [ ] `cargo test --test cli_dry_run` passes
- [ ] Manual test: `splice patch --dry-run` with changes returns exit code 1
- [ ] Manual test: `splice patch --dry-run` without changes returns exit code 0
- [ ] Manual test: `splice delete --dry-run` for existing symbol returns exit code 1
- [ ] Manual test: `splice delete --dry-run` for non-existent symbol returns exit code 0
- [ ] Bash test: `splice patch --dry-run ...; echo $?` shows 1 for changes
</verification>

<success_criteria>
Exit codes follow git diff --exit-code convention, enabling scripting and pre-commit hook integration.
</success_criteria>

<output>
After completion, create `.planning/phases/13-dry-run-diff/13-05-SUMMARY.md`
</output>
