---
phase: 02-sqlitegraph-upgrade
plan: 03
type: execute
depends_on: [02]
files_modified: [src/graph/mod.rs, src/graph/schema.rs, src/error.rs, src/resolve/mod.rs]
autonomous: true
---

<objective>
Migrate code to SQLiteGraph 1.0 Native V2 API, fixing all compilation errors from Plan 02-02.

Purpose: Update the code that uses SQLiteGraph types and methods to work with v1.0 Native V2 backend. This is the core migration work.

Output: All files updated, cargo check passes, cargo test passes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-sqlitegraph-upgrade/02-01-SUMMARY.md
@.planning/phases/02-sqlitegraph-upgrade/02-02-SUMMARY.md
@docs/API.md
@src/graph/mod.rs
@src/graph/schema.rs
@src/error.rs
@src/resolve/mod.rs

**From Plan 02-01 Discovery - Expected Changes:**

| File | Line | Change |
|------|------|--------|
| src/graph/mod.rs | 34 | `GraphConfig::sqlite()` → `GraphConfig::native()` |
| src/graph/schema.rs | 7 | Verify `use sqlitegraph::{Label, PropertyKey}` works |
| src/error.rs | 28 | Verify `#[from] sqlitegraph::SqliteGraphError` works |
| src/resolve/mod.rs | 13 | Verify `use sqlitegraph::NodeId` works |

**Type Compatibility (from docs/API.md):**
- `NodeSpec { kind, name, file_path, data }` - unchanged
- `EdgeSpec { from, to, edge_type, data }` - unchanged
- `NodeId` - unchanged (wraps u64)
- `Label` - unchanged (wraps String)
- `PropertyKey` - unchanged (wraps String)
- `GraphBackend` trait - verify methods still match

**Native V2 vs SQLite Backend:**

Current code (0.2.11) uses what v1.0 calls the "Native V2" API:
- `GraphConfig::sqlite()` → becomes `GraphConfig::native()`
- `open_graph(path, &cfg)` - same function name

**Expected No-Change Files:**
- src/graph/schema.rs - Label/PropertyKey unchanged
- src/error.rs - SqliteGraphError unchanged
- src/resolve/mod.rs - NodeId unchanged

**Key Change Location:**

src/graph/mod.rs:34
```rust
// Before
let cfg = sqlitegraph::GraphConfig::sqlite();

// After
let cfg = sqlitegraph::GraphConfig::native();
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update GraphConfig in src/graph/mod.rs</name>
  <files>src/graph/mod.rs</files>
  <action>Change line 34 from `GraphConfig::sqlite()` to `GraphConfig::native()`.

Use Edit tool:
- Old: `let cfg = sqlitegraph::GraphConfig::sqlite();`
- New: `let cfg = sqlitegraph::GraphConfig::native();`

This is the only code change expected based on API documentation.</action>
  <verify>Line 34 shows GraphConfig::native()</verify>
  <done>GraphConfig updated to use Native V2 backend</done>
</task>

<task type="auto">
  <name>Task 2: Verify Label and PropertyKey imports in src/graph/schema.rs</name>
  <files>src/graph/schema.rs</files>
  <action>Run cargo check to verify Label and PropertyKey types still work.

Execute:
```bash
cargo check 2>&1 | grep -A 3 "schema.rs"
```

If there are errors, they may be:
- Type constructor changes (e.g., `Label("...")` vs `Label::new("...")`)
- Module path changes

Fix any errors found. Expected: No changes needed since these types are unchanged in v1.0.</action>
  <verify>cargo check shows no errors in schema.rs</verify>
  <done>Label and PropertyKey imports verified working</done>
</task>

<task type="auto">
  <name>Task 3: Verify error type integration in src/error.rs</name>
  <files>src/error.rs</files>
  <action>Run cargo check to verify SqliteGraphError integration still works.

Execute:
```bash
cargo check 2>&1 | grep -A 3 "error.rs"
```

Line 28 has:
```rust
Graph(#[from] sqlitegraph::SqliteGraphError),
```

If there are errors, document them. Expected: No changes needed since the error type is unchanged in v1.0.</action>
  <verify>cargo check shows no errors related to error type</verify>
  <done>SqliteGraphError integration verified</done>
</task>

<task type="auto">
  <name>Task 4: Verify NodeId import in src/resolve/mod.rs</name>
  <files>src/resolve/mod.rs</files>
  <action>Run cargo check to verify NodeId type still works.

Execute:
```bash
cargo check 2>&1 | grep -A 3 "resolve/mod.rs"
```

Line 13 has:
```rust
use sqlitegraph::NodeId;
```

If there are errors, document them. Expected: No changes needed since NodeId is unchanged in v1.0.</action>
  <verify>cargo check shows no errors related to NodeId</verify>
  <done>NodeId import verified working</done>
</task>

<task type="auto">
  <name>Task 5: Fix GraphBackend trait method calls</name>
  <files>src/graph/mod.rs</files>
  <action>Check for any GraphBackend trait method calls that may have changed.

The GraphBackend trait methods used:
- `backend.insert_node(node_spec)` - line 78, 131, 197
- `backend.insert_edge(edge_spec)` - line 141
- `backend.get_node(node_id)` - line 229, 254

Run cargo check to verify these methods still exist:
```bash
cargo check 2>&1 | tee /tmp/cargo-check-02-03.log
```

If there are errors, fix them based on the actual v1.0 API.

Document any unexpected changes in the summary.</action>
  <verify>All GraphBackend method calls compile successfully</verify>
  <done>GraphBackend trait methods verified or fixed</done>
</task>

<task type="auto">
  <name>Task 6: Run full test suite</name>
  <files></files>
  <action>After all compilation errors are fixed, run the full test suite.

Execute:
```bash
cargo test --lib 2>&1 | tee /tmp/cargo-test-02-03.log
```

All tests should pass. If any tests fail:
1. Investigate the failure
2. Determine if it's related to SQLiteGraph v1.0 changes
3. Fix the issue or document why test needs updating

Key tests to verify:
- Graph insertion tests (src/graph/mod.rs tests)
- Schema tests (src/graph/schema.rs tests)
- Resolution tests (src/resolve/mod.rs tests)</action>
  <verify>All tests pass (111 tests as per Phase 1 baseline)</verify>
  <done>Full test suite passes with SQLiteGraph v1.0</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] src/graph/mod.rs:34 updated to GraphConfig::native()
- [ ] src/graph/schema.rs compiles without errors
- [ ] src/error.rs compiles without errors
- [ ] src/resolve/mod.rs compiles without errors
- [ ] All GraphBackend method calls verified
- [ ] cargo check passes without errors
- [ ] cargo test --lib passes (111+ tests)
</verification>

<success_criteria>
- Code compiles with cargo check
- All GraphBackend trait methods work
- Full test suite passes
- No unexpected API changes encountered
</success_criteria>

<output>
After completion, create `.planning/phases/02-sqlitegraph-upgrade/02-03-SUMMARY.md`

Include in SUMMARY.md:
- Lines changed per file
- Any unexpected API changes found
- Compilation errors encountered and fixed
- Test results (pass/fail counts)
- Any test fixes required
