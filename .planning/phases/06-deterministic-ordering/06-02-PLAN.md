---
phase: 06-deterministic-ordering
plan: 02
type: execute
depends_on: ["01"]
files_modified: [src/main.rs]
autonomous: true
---

<objective>
Add sorting to main command output (patch, delete, plan) for deterministic JSON.

Purpose: Ensure patch, delete, and plan command outputs are sorted consistently for reproducible JSON.
Output: Sorted spans, files_affected in structured JSON output.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/main.rs
@src/output.rs

# Depends on:
@.planning/phases/06-deterministic-ordering/06-01-PLAN.md - Ord implementations for sorting

# Sorting locations in main.rs:
- Delete command spans: ~line 406 (spans.push then spans.push for refs)
- Patch command spans: ~line 650-700 (similar pattern)
- Plan command files_affected: ~line 977 (vec![plan_path...])
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sort DeleteResult.spans before output</name>
  <files>src/main.rs</files>
  <action>
    In execute_delete() function, after creating spans Vec (~line 406):
    Add `spans.sort();` before creating DeleteResult.

    The spans Vec contains:
    1. Definition span (from resolve_symbol)
    2. Reference spans (from ref_set.references)

    Current order: definition first, then refs (unsorted).
    After sort: sorted by file_path then byte_start (from SpanResult Ord in 06-01).
  </action>
  <verify>delete command JSON output has sorted spans</verify>
  <done>DeleteResult.spans sorted deterministically</done>
</task>

<task type="auto">
  <name>Task 2: Sort PatchResult.spans before output</name>
  <files>src/main.rs</files>
  <action>
    Find execute_patch() or execute_single_patch() function where PatchResult is created.
    Locate where spans Vec is populated and add `spans.sort();` before creating PatchResult.

    Patch command may have multiple spans (multi-symbol patches).
    Sort ensures consistent output order.
  </action>
  <verify>patch command JSON output has sorted spans</verify>
  <done>PatchResult.spans sorted deterministically</done>
</task>

<task type="auto">
  <name>Task 3: Sort PlanResult.files_affected before output</name>
  <files>src/main.rs</files>
  <action>
    In execute_plan() function, find line ~977:
    ```
    files_affected: vec![plan_path.to_string_lossy().to_string()],
    ```

    Replace with:
    ```
    files_affected: {
        let mut files = vec![plan_path.to_string_lossy().to_string()];
        files.sort();
        files
    },
    ```

    Currently only contains plan_path (single element), but sort for consistency
    in case multiple files are affected in future.
  </action>
  <verify>plan command JSON output has sorted files_affected</verify>
  <done>PlanResult.files_affected sorted deterministically</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo check passes
- [ ] cargo test passes
- [ ] Delete JSON output spans sorted by file_path then byte_start
- [ ] Patch JSON output spans sorted by file_path then byte_start
- [ ] Plan JSON output files_affected sorted
</verification>

<success_criteria>

- All main command outputs sorted deterministically
- Test JSON output verifies ordering
  </success_criteria>

<output>
After completion, create `.planning/phases/06-deterministic-ordering/06-02-SUMMARY.md`
</output>
