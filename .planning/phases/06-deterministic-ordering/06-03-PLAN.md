---
phase: 06-deterministic-ordering
plan: 03
type: execute
depends_on: ["01"]
files_modified: [src/main.rs, src/resolve/references/mod.rs]
autonomous: true
---

<objective>
Add sorting to query, batch, apply_files commands and reference finding for deterministic output.

Purpose: Ensure remaining commands (query, batch, apply_files) and internal reference finding produce sorted output.
Output: Sorted symbols, files, labels, and references.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/main.rs
@src/resolve/references/mod.rs

# Depends on:
@.planning/phases/06-deterministic-ordering/06-01-PLAN.md - Ord implementations for sorting

# Sorting locations:
- Query command symbols: ~line 1227 (symbols_data from results)
- Batch/apply_files files: ~line 816 (file_results Vec)
- ApplyFilesResult.files: spans need sorting
- ReferenceSet.references: in resolve/references/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sort QueryResult.symbols before output</name>
  <files>src/main.rs</files>
  <action>
    In execute_query() function, find where results are processed (~line 1213).
    The `results` Vec from `integration.query_by_labels()` is already processed.

    Add sorting to results before building symbols_data:
    ```
    let mut results = integration.query_by_labels(&labels_ref)?;
    results.sort_by_key(|r| (&r.file_path, r.byte_start));
    ```

    This ensures QueryResult.symbols (if using structured output) would be sorted.
    The current code uses symbols_data for JSON but doesn't use QueryResult type directly.
    Add sorting for consistency.
  </action>
  <verify>query command output sorted by file_path then byte_start</verify>
  <done>Query results sorted deterministically</done>
</task>

<task type="auto">
  <name>Task 2: Sort ApplyFilesResult.files and spans</name>
  <files>src/main.rs</files>
  <action>
    In execute_batch() function (around line 816):

    1. Sort file_results before creating ApplyFilesResult:
       ```
       file_results.sort();
       ```
       Uses FilePatternResult Ord from 06-01 (sorts by file path).

    2. Sort spans within each FilePatternResult:
       After the spans Vec is populated (line 819-830), add:
       ```
       spans.sort();
       ```
       Uses SpanResult Ord from 06-01.
  </action>
  <verify>batch/apply_files JSON output has sorted files and spans</verify>
  <done>ApplyFilesResult.files and spans sorted deterministically</done>
</task>

<task type="auto">
  <name>Task 3: Sort ReferenceSet.references</name>
  <files>src/resolve/references/mod.rs</files>
  <action>
    In the ReferenceSet struct and its creation/usage:

    The references Vec<Reference> should be sorted for consistent output.
    Find where ReferenceSet is created and add:
    ```
    references.sort_by_key(|r| (&r.file_path, r.byte_start));
    ```

    Locations to check:
    - src/resolve/references/mod.rs: ReferenceSet struct
    - src/resolve/references/rust.rs: find_all_references() returns Vec<Reference>

    The rust.rs already sorts descending for deletion (line 53).
    Add ascending sort for storage/output purposes.
  </action>
  <verify>ReferenceSet.references sorted in output</verify>
  <done>References sorted deterministically</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo check passes
- [ ] cargo test passes
- [ ] Query output sorted by file_path then byte_start
- [ ] Apply files output sorted by file path
- [ ] Spans within each file sorted by byte_start
- [ ] References sorted deterministically
</verification>

<success_criteria>

- All remaining outputs sorted deterministically
- Consistent ordering across all commands
  </success_criteria>

<output>
After completion, create `.planning/phases/06-deterministic-ordering/06-03-SUMMARY.md`
</output>
