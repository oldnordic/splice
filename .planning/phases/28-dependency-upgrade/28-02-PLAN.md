---
phase: 28-dependency-upgrade
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - Cargo.toml
  - src/symbol_id.rs
  - src/lib.rs
autonomous: true

must_haves:
  truths:
    - "New BLAKE3 SymbolId generation produces 32-character hex IDs"
    - "Legacy SHA-256 SymbolId generation still produces 16-character hex IDs"
    - "SymbolId enum accepts both 16-char and 32-char formats via parse()"
    - "All existing symbol_id module tests pass"
  artifacts:
    - path: "src/symbol_id.rs"
      provides: "Dual-format SymbolId enum with BLAKE3 support"
      exports: ["SymbolId::parse()", "SymbolId::generate_v1()", "SymbolId::generate_v2()"]
    - path: "Cargo.toml"
      provides: "blake3 dependency specification"
      contains: "blake3 = \"1.5\""
  key_links:
    - from: "src/symbol_id.rs"
      to: "src/graph/magellan_integration.rs"
      via: "SymbolId type"
      pattern: "use crate::symbol_id::SymbolId"
---

<objective>
Add BLAKE3 dependency and implement dual-format SymbolId enum that supports both legacy 16-char SHA-256 IDs (V1) and new 32-char BLAKE3 IDs (V2). This enables backward compatibility while providing the unambiguous identifier benefits of BLAKE3.

Purpose: Implement SymbolId format migration from SHA-256 (16-char) to BLAKE3 (32-char) with dual-format support during transition period
Output: SymbolId enum with V1/V2 variants, BLAKE3 dependency added, all tests passing
</objective>

<execution_context>
@/home/feanor/.claude/get-shit-done/workflows/execute-plan.md
@/home/feanor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-dependency-upgrade/28-RESEARCH.md

# Existing SymbolId implementation (V1 only)
@/home/feanor/Projects/splice/src/symbol_id.rs
</context>

<tasks>

<task type="auto">
  <name>Add blake3 dependency to Cargo.toml</name>
  <files>Cargo.toml</files>
  <action>Add the blake3 dependency to /home/feanor/Projects/splice/Cargo.toml in the [dependencies] section (after sha2 = "0.10" on line 54):

blake3 = "1.5"

This provides the BLAKE3 hashing algorithm for 32-char SymbolId generation. Keep sha2 = "0.10" for backward compatibility with V1 ID generation.</action>
  <verify>cargo check --all-targets compiles successfully</verify>
  <done>Cargo.toml contains blake3 = "1.5" dependency</done>
</task>

<task type="auto">
  <name>Implement dual-format SymbolId enum</name>
  <files>src/symbol_id.rs</files>
  <action>Refactor src/symbol_id.rs to support dual-format SymbolId. Key changes:

1. Convert SymbolId from newtype struct to enum:
   pub enum SymbolId {
       V1(String),  // 16-char SHA-256 (legacy)
       V2(String),  // 32-char BLAKE3 (new)
   }

2. Update SymbolIdError to accept both 16 and 32 char lengths:
   - InvalidLength { length: usize } now errors only if length != 16 AND != 32
   - All validation logic accepts both formats

3. Implement SymbolId::parse() method:
   pub fn parse(input: &str) -> Result<Self, SymbolIdError> {
       match input.len() {
           16 => Ok(SymbolId::V1(input.to_string())),
           32 => Ok(SymbolId::V2(input.to_string())),
           _ => Err(SymbolIdError::InvalidLength { length: input.len() }),
       }
   }

4. Add generate_v2() for BLAKE3 IDs:
   pub fn generate_v2(name: &str, file_path: &str, byte_start: usize) -> SymbolId {
       use blake3::Hasher;
       let mut hasher = Hasher::new();
       hasher.update(name.as_bytes());
       hasher.update(b":");
       hasher.update(file_path.as_bytes());
       hasher.update(b":");
       hasher.update(byte_start.to_be_bytes());
       let hash = hasher.finalize();
       SymbolId::V2(hash.to_hex().as_str().to_string())
   }

5. Rename existing generate_symbol_id() to generate_v1() for clarity:
   pub fn generate_v1(name: &str, file_path: &str, byte_start: usize) -> SymbolId

6. Add convenience wrapper generate_symbol_id() that defaults to V2:
   pub fn generate_symbol_id(name: &str, file_path: &str, byte_start: usize) -> SymbolId {
       generate_v2(name, file_path, byte_start)  // Prefer BLAKE3 for new IDs
   }

7. Update all trait implementations (Display, AsRef, TryFrom, Hash) to work with both V1 and V2 variants

8. Update module documentation to reflect dual-format support

Reference the existing SHA-256 generation code in lines 247-268 as the basis for generate_v1().</action>
  <verify>cargo test --lib symbol_id passes all tests</verify>
  <done>SymbolId enum with V1/V2 variants, both format generation methods working</done>
</task>

<task type="auto">
  <name>Update lib.rs re-exports for dual-format SymbolId</name>
  <files>src/lib.rs</files>
  <action>Verify src/lib.rs has the correct re-export for SymbolId:

pub mod symbol_id;

The symbol_id module should export:
- SymbolId enum (V1/V2 variants)
- generate_symbol_id() (defaults to V2 BLAKE3)
- generate_v1() (legacy SHA-256)
- generate_v2() (new BLAKE3)
- generate_execution_id() (unchanged)
- SymbolIdError (updated for dual-format)

No changes needed if the existing pub mod symbol_id; is in place - just verify it compiles.</action>
  <verify>cargo check --lib succeeds, symbol_id exports are accessible</verify>
  <done>lib.rs correctly exports dual-format SymbolId module</done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Run cargo test --lib symbol_id to verify all symbol_id tests pass
2. Run cargo check --all to verify no compilation errors
3. Test that V1 IDs are 16 chars: cargo test generate_v1
4. Test that V2 IDs are 32 chars: cargo test generate_v2
5. Verify parse() accepts both formats
</verification>

<success_criteria>
1. blake3 = "1.5" in Cargo.toml
2. SymbolId enum with V1 and V2 variants exists
3. generate_v1() produces 16-char IDs (SHA-256)
4. generate_v2() produces 32-char IDs (BLAKE3)
5. parse() accepts both 16-char and 32-char strings
6. All symbol_id tests pass
7. cargo check --all succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/28-dependency-upgrade/28-02-SUMMARY.md`
</output>
