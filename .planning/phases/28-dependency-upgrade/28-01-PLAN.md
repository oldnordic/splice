---
phase: 28-dependency-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - Cargo.lock
autonomous: true

must_haves:
  truths:
    - "cargo build succeeds after dependency upgrade"
    - "cargo test --all compiles all tests"
    - "Magellan 2.0.0 and SQLiteGraph 1.3.0 are resolved in Cargo.lock"
  artifacts:
    - path: "Cargo.toml"
      provides: "Dependency specifications"
      contains: "magellan = \"2.0.0\""
    - path: "Cargo.lock"
      provides: "Locked dependency versions"
      contains: "name: \"magellan\" version: \"2.0.0\""
  key_links:
    - from: "Cargo.toml"
      to: "crates.io registry"
      via: "cargo update"
      pattern: "magellan.*2.0.0"
---

<objective>
Upgrade Magellan from 0.5.3 to 2.0.0 and SQLiteGraph from 1.2.7 to 1.3.0. This is a prerequisite for BLAKE3 SymbolId support in Phase 28.

Purpose: Enable new graph algorithm capabilities and BLAKE3 SymbolId format from Magellan 2.0.0
Output: Updated Cargo.toml with new dependency versions, Cargo.lock with resolved versions
</objective>

<execution_context>
@/home/feanor/.claude/get-shit-done/workflows/execute-plan.md
@/home/feanor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Current dependency state
@/home/feanor/Projects/splice/Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Update Cargo.toml dependencies</name>
  <files>Cargo.toml</files>
  <action>Update the dependency versions in /home/feanor/Projects/splice/Cargo.toml:

1. Change magellan from "0.5.3" to "2.0.0" (line 25)
2. Change sqlitegraph from "1.2.7" to "1.3.0" (line 27)
3. Keep sha2 = "0.10" for backward compatibility with 16-char ID generation

Do NOT modify any other dependencies. The blake3 dependency will be added in plan 28-02.</action>
  <verify>cargo check --all-targets succeeds without errors</verify>
  <done>Cargo.toml contains magellan = "2.0.0" and sqlitegraph = "1.3.0"</done>
</task>

<task type="auto">
  <name>Update Cargo.lock with cargo update</name>
  <files>Cargo.lock</files>
  <action>Run cargo update to resolve and update Cargo.lock with the new transitive dependencies from Magellan 2.0.0 and SQLiteGraph 1.3.0.

If cargo update reports conflicts, use:
cargo update -p magellan --precise 2.0.0
cargo update -p sqlitegraph --precise 1.3.0

This ensures transitive dependencies are also updated correctly.</action>
  <verify>cargo build --dry-run shows magellan 2.0.0 and sqlitegraph 1.3.0 in build plan</verify>
  <done>Cargo.lock contains resolved magellan 2.0.0 and sqlitegraph 1.3.0 versions</done>
</task>

<task type="auto">
  <name>Verify compilation and prepare for next plans</name>
  <files>src/symbol_id.rs</files>
  <action>Run cargo check --all-targets to verify the dependency upgrade compiles. Expect some compilation errors due to API changes in Magellan 2.0.0 - these will be addressed in subsequent plans (28-02, 28-03).

DO NOT fix any compilation errors yet - just verify the dependency versions are correctly resolved. The SymbolId enum implementation will be added in plan 28-02.

If cargo check passes completely, note that Magellan 2.0.0 has backward-compatible APIs and we can proceed to add BLAKE3 support in plan 28-02.</action>
  <verify>cargo check --all-targets completes (either with expected errors or success)</verify>
  <done>Dependency versions verified, ready for SymbolId dual-format implementation</done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Run cargo tree | grep -E "(magellan|sqlitegraph)" to verify versions
2. Run cargo check to confirm no unexpected breaking changes
3. Check that sha2 dependency is still present (needed for backward compatibility)
</verification>

<success_criteria>
1. cargo tree shows magellan v2.0.0 and sqlitegraph v1.3.0
2. Cargo.lock contains the new resolved versions
3. cargo check completes (with or without expected API errors)
4. sha2 = "0.10" is still in Cargo.toml
</success_criteria>

<output>
After completion, create `.planning/phases/28-dependency-upgrade/28-01-SUMMARY.md`
</output>
