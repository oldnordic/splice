---
phase: 28-dependency-upgrade
plan: 03
type: execute
wave: 3
depends_on: [28-02]
files_modified:
  - src/output.rs
  - src/graph/magellan_integration.rs
  - tests/id_format_tests.rs
autonomous: true

must_haves:
  truths:
    - "All JSON responses include 32-char BLAKE3 symbol_id fields for new operations"
    - "JSON schema includes id_format field ('v1' or 'v2') for client detection"
    - "Existing 16-char SHA-256 IDs from databases are preserved in JSON output"
    - "All id_format_tests pass with both 16-char and 32-char ID validation"
  artifacts:
    - path: "src/output.rs"
      provides: "JSON response types with dual-format SymbolId support"
      exports: ["MagellanSymbol", "id_format field"]
    - path: "src/graph/magellan_integration.rs"
      provides: "Updated find_symbol_by_id() with dual-format support"
      contains: "generate_v2"
  key_links:
    - from: "src/graph/magellan_integration.rs"
      to: "src/symbol_id.rs"
      via: "generate_v1() and generate_v2() calls for backward compatibility"
      pattern: "crate::symbol_id::generate_(v1|v2)"
    - from: "src/output.rs"
      to: "CLI JSON consumers"
      via: "symbol_id and id_format fields"
      pattern: "symbol_id.*id_format"
---

<objective>
Update JSON output to use 32-char BLAKE3 SymbolIds while preserving backward compatibility with 16-char SHA-256 IDs. Add id_format field to JSON responses so clients can detect which format is being used.

Purpose: Ensure all JSON responses use new 32-char BLAKE3 IDs for new operations while maintaining backward compatibility with existing databases containing 16-char IDs
Output: Updated output.rs with id_format field, magellan_integration.rs using generate_v2(), all tests passing
</objective>

<execution_context>
@/home/feanor/.claude/get-shit-done/workflows/execute-plan.md
@/home/feanor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-dependency-upgrade/28-RESEARCH.md

# JSON output types (need dual-format SymbolId)
@/home/feanor/Projects/splice/src/output.rs

# Magellan integration layer (uses generate_symbol_id)
@/home/feanor/Projects/splice/src/graph/magellan_integration.rs

# Existing ID format tests
@/home/feanor/Projects/splice/tests/id_format_tests.rs
</context>

<tasks>

<task type="auto">
  <name>Update MagellanSymbol type with id_format field</name>
  <files>src/output.rs</files>
  <action>Update the MagellanSymbol struct in src/output.rs to include id_format field. Find the struct around lines 1073-1095 and add:

/// ID format hint for clients ("v1" for 16-char SHA-256, "v2" for 32-char BLAKE3)
#[serde(skip_serializing_if = "Option::is_none")]
pub id_format: Option<String>

Also update the From implementation to set this field based on the SymbolId variant:
- V1(16-char) -> id_format = Some("v1".to_string())
- V2(32-char) -> id_format = Some("v2".to_string())

The symbol_id field should now accept both 16-char and 32-char values.

Search for all structs with symbol_id fields and add id_format to:
- MagellanSymbol
- SymbolExport
- Any other types with symbol_id field</action>
  <verify>cargo check --lib succeeds, JSON serialization includes id_format</verify>
  <done>MagellanSymbol and related types have id_format field</done>
</task>

<task type="auto">
  <name>Update magellan_integration to use generate_v2() for new IDs</name>
  <files>src/graph/magellan_integration.rs</files>
  <action>Update src/graph/magellan_integration.rs to prefer BLAKE3 IDs for new operations:

1. In find_symbol_by_id() around line 418, update to use generate_v2():
   Change: let generated_id = generate_symbol_id(&name, &file_path, byte_start);
   To: Use crate::symbol_id::{generate_v1, generate_v2};

   For ID comparison, support both formats:
   - Try V2 (32-char) comparison first
   - Fall back to V1 (16-char) comparison if V2 doesn't match

2. Update the function documentation to reflect dual-format support:
   "Find symbol by 16-char SHA-256 or 32-char BLAKE3 symbol ID."

3. For new symbol generation (not in this file, but for future reference), use generate_v2() by default.

The key is that find_symbol_by_id() must be able to look up symbols by either format since databases may contain both. When comparing incoming IDs, try parsing as V2 (32-char) first, then V1 (16-char) for backward compatibility.</action>
  <verify>cargo test magellan_integration passes</verify>
  <done>magellan_integration.rs uses dual-format SymbolId</done>
</task>

<task type="auto">
  <name>Update id_format tests for dual-format support</name>
  <files>tests/id_format_tests.rs</files>
  <action>Update tests/id_format_tests.rs to validate both 16-char and 32-char SymbolId formats:

1. Add test for V1 (16-char) format:
   - Verify 16-char IDs are accepted
   - Verify id_format = "v1" in JSON output
   - Test backward compatibility reading old databases

2. Add test for V2 (32-char) format:
   - Verify 32-char IDs are accepted
   - Verify id_format = "v2" in JSON output
   - Test new operations produce 32-char IDs

3. Update existing tests to be format-agnostic:
   - Replace exact length assertions with regex: r"^[0-9a-f]{16,32}$"
   - Use subset assertions for JSON fields instead of exact string matching
   - Test that both formats validate correctly

4. Add test for SymbolId::parse() dual-format detection:
   - 16-char input -> V1 variant
   - 32-char input -> V2 variant
   - Other lengths -> SymbolIdError::InvalidLength</action>
  <verify>cargo test id_format_tests passes all tests</verify>
  <done>id_format_tests validate both 16-char and 32-char formats</done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Run cargo test id_format_tests -- --nocapture to see detailed test output
2. Run cargo test magellan_integration to verify integration layer
3. Check JSON output includes id_format field: cargo run -- find --symbol-id <test-id> --output json
4. Verify both 16-char and 32-char IDs are accepted in queries
</verification>

<success_criteria>
1. MagellanSymbol and related types have id_format field
2. id_format is "v1" for 16-char IDs, "v2" for 32-char IDs
3. find_symbol_by_id() accepts both formats
4. All id_format_tests pass
5. JSON serialization includes symbol_id and id_format fields
6. New operations generate 32-char BLAKE3 IDs
</success_criteria>

<output>
After completion, create `.planning/phases/28-dependency-upgrade/28-03-SUMMARY.md`
</output>
