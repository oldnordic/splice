---
phase: 14-context-flags
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - src/cli/mod.rs
autonomous: true

must_haves:
  truths:
    - "Get command has -A, -B, -C context flags"
    - "ApplyFiles command has -A, -B, -C context flags"
    - "All 6 commands (Delete, Patch, Query, Get, ApplyFiles, Plan) have consistent context flag interfaces"
  artifacts:
    - path: "src/cli/mod.rs"
      provides: "-A, -B, -C flags for remaining commands"
      contains: "context_after, context_before, context_both fields for Get and ApplyFiles commands"
  key_links:
    - from: "src/cli/mod.rs"
      to: "context_after parameter"
      via: "clap short='A' argument definition for Get and ApplyFiles"
      pattern: "#\[arg.*short.*=.*'A'.*\]"
---

<objective>
Add -A, -B, -C context flags to remaining CLI commands (Get, ApplyFiles).

Purpose: Complete the context flag coverage across all commands that support context, ensuring consistency in user interface.

Output: Get and ApplyFiles commands have -A, -B, -C flags matching other commands.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-context-flags/14-01-PLAN.md

# Existing infrastructure
@src/cli/mod.rs - Plan 14-01 added -A, -B, -C to Delete, Patch, Query
@src/cli/mod.rs - Get command has context_lines (line 265-268)
@src/cli/mod.rs - ApplyFiles command has context_lines (line 195-197)

# Commands that need context flags
- Get command (line 249-273): Retrieves code chunks by byte range
- ApplyFiles command (line 178-214): Pattern replacement across files
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add -A, -B, -C flags to Get command</name>
  <files>src/cli/mod.rs</files>
  <action>
In src/cli/mod.rs, modify the Get command (around line 249-273):

1. REMOVE the existing context_lines field (line 265-268):
```rust
/// Number of context lines before/after spans (default: 3).
#[arg(long, value_name = "N", default_value = "3")]
context_lines: usize,
```

2. ADD three new context flag fields in its place:
```rust
/// Number of context lines after the match.
#[arg(short = 'A', long, value_name = "N", default_value = "0")]
context_after: usize,

/// Number of context lines before the match.
#[arg(short = 'B', long, value_name = "N", default_value = "0")]
context_before: usize,

/// Number of context lines before and after the match (default: 3).
#[arg(short = 'C', long, value_name = "N", default_value = "3")]
context_both: usize,
```
  </action>
  <verify>grep -A "context_after\|context_before\|context_both" src/cli/mod.rs | grep -A1 "Get {" shows the three new fields</verify>
  <done>Get command has -A, -B, -C flags with correct defaults</done>
</task>

<task type="auto">
  <name>Task 2: Add -A, -B, -C flags to ApplyFiles command</name>
  <files>src/cli/mod.rs</files>
  <action>
In src/cli/mod.rs, modify the ApplyFiles command (around line 178-214):

1. REMOVE the existing context_lines field (line 195-197):
```rust
/// Number of context lines before/after spans (default: 3).
#[arg(long, value_name = "N", default_value = "3")]
context_lines: usize,
```

2. ADD three new context flag fields in its place:
```rust
/// Number of context lines after the match.
#[arg(short = 'A', long, value_name = "N", default_value = "0")]
context_after: usize,

/// Number of context lines before the match.
#[arg(short = 'B', long, value_name = "N", default_value = "0")]
context_before: usize,

/// Number of context lines before and after the match (default: 3).
#[arg(short = 'C', long, value_name = "N", default_value = "3")]
context_both: usize,
```
  </action>
  <verify>grep -A "context_after\|context_before\|context_both" src/cli/mod.rs | grep -A1 "ApplyFiles {" shows the three new fields</verify>
  <done>ApplyFiles command has -A, -B, -C flags with correct defaults</done>
</task>

<task type="auto">
  <name>Task 3: Verify all context flags are defined consistently</name>
  <files>src/cli/mod.rs</files>
  <action>
Run grep to verify all commands have consistent context flag definitions:

```bash
# Verify all -A flags
grep -n "short = 'A'" src/cli/mod.rs

# Verify all -B flags
grep -n "short = 'B'" src/cli/mod.rs

# Verify all -C flags
grep -n "short = 'C'" src/cli/mod.rs
```

Expected output:
- 5 matches for -A (Delete, Patch, Query, Get, ApplyFiles)
- 5 matches for -B (Delete, Patch, Query, Get, ApplyFiles)
- 5 matches for -C (Delete, Patch, Query, Get, ApplyFiles)

All should have:
- default_value = "0" for -A and -B
- default_value = "3" for -C
  </action>
  <verify>grep -c "short = 'A'" src/cli/mod.rs returns 5, same for -B and -C</verify>
  <done>All 5 commands have consistent -A, -B, -C flags</done>
</task>

</tasks>

<verification>
1. Run `cargo run -- get --help` and verify -A, -B, -C flags appear
2. Run `cargo run -- apply-files --help` and verify -A, -B, -C flags appear
3. Run grep commands to verify all 5 commands have all three flags
4. Verify compilation succeeds: `cargo check`
</verification>

<success_criteria>
1. Get command has -A, -B, -C flags with correct defaults
2. ApplyFiles command has -A, -B, -C flags with correct defaults
3. All 5 affected commands have identical flag signatures
4. Code compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/14-context-flags/14-02-SUMMARY.md`
</output>
