---
phase: 19-critical-error-handling
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [src/ingest/imports/java.rs]
autonomous: true

must_haves:
  truths:
    - "All unwrap() calls in test code replaced with proper error handling"
    - "All java tests still pass after changes"
  artifacts:
    - path: "src/ingest/imports/java.rs"
      provides: "Java import extraction with safe error handling"
      min_lines: 209
  key_links:
    - from: "src/ingest/imports/java.rs tests"
      to: "extract_java_imports"
      via: "? operator instead of unwrap()"
      pattern: "\\?\\s*$"
---

<objective>
Fix test code error handling in java.rs.

Purpose: Replace 6 unwrap() calls in test code (ERROR-06) with proper Result propagation.
Output: Java import extraction with proper error handling patterns.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/BUG_ANALYSIS.md

@src/ingest/imports/java.rs
</context>

<tasks>

<task type="auto">
  <name>Replace unwrap() calls in all java.rs test functions</name>
  <files>src/ingest/imports/java.rs</files>
  <action>
    There are 6 test functions with unwrap() calls at lines: 143, 155, 167, 179, 191, 203.

    For each test function, apply this pattern:

    1. Change the function signature from:
    ```rust
    #[test]
    fn test_extract_simple_import() {
    ```
    To:
    ```rust
    #[test]
    fn test_extract_simple_import() -> Result<(), Box<dyn std::error::Error>> {
    ```

    2. Replace each `result.unwrap()` with:
    ```rust
    let imports = result?;
    ```

    Functions to update:
    - test_extract_simple_import (line 138-147) - unwrap at 143
    - test_extract_static_import (line 150-159) - unwrap at 155
    - test_extract_wildcard_import (line 162-171) - unwrap at 167
    - test_extract_static_wildcard_import (line 174-183) - unwrap at 179
    - test_extract_multiple_imports (line 186-195) - unwrap at 191
    - test_import_has_byte_span (line 198-207) - unwrap at 203

    The ? operator provides better error messages and is the standard Rust testing pattern.
  </action>
  <verify>cargo test --lib ingest::imports::java</verify>
  <done>All 6 unwrap() calls in tests replaced with ? operator</done>
</task>

</tasks>

<verification>
1. Read src/ingest/imports/java.rs and confirm:
   - No unwrap() calls remain in test code (lines 133-208)

2. Run tests to ensure no regression:
   ```bash
   cargo test --lib ingest::imports::java
   ```

3. Count unwrap() in the test module should be zero:
   ```bash
   grep -n "unwrap()" src/ingest/imports/java.rs | grep -E "^1[3-9][0-9]:|^20[0-9]:"
   ```
   Should return nothing.
</verification>

<success_criteria>
- No unwrap() in java.rs test code (6 locations replaced)
- All java tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/19-critical-error-handling/19-04-SUMMARY.md` with:
- All functions modified with line numbers
- Test results
</output>
