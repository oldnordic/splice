---
phase: 19-critical-error-handling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/ingest/imports/python.rs]
autonomous: true

must_haves:
  truths:
    - "path.first().unwrap() replaced with safe pattern at line 95"
    - "name_path.last().unwrap() replaced with safe pattern at line 201"
    - "Production code has no unwrap() on first() or last() without empty checks"
    - "All existing tests still pass after changes"
  artifacts:
    - path: "src/ingest/imports/python.rs"
      provides: "Python import extraction with safe first()/last() access"
      contains: "path.first().cloned()"
      min_lines: 315
  key_links:
    - from: "src/ingest/imports/python.rs:95"
      to: "extract_dotted_name_path"
      via: "cloned() instead of unwrap()"
      pattern: "first\\(\\)\\.cloned\\(\\)|last\\(\\)\\.cloned\\(\\)"
---

<objective>
Fix critical panic-risk bugs in python.rs by replacing unsafe unwrap() calls on first() and last() with safe alternatives.

Purpose: Eliminate panic risk from empty collections in production code (ERROR-01, ERROR-02 from bug analysis).
Output: Python import extraction that uses Option chaining instead of unwrap().
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/BUG_ANALYSIS.md

@src/ingest/imports/python.rs
</context>

<tasks>

<task type="auto">
  <name>Fix unwrap() on first() at line 95</name>
  <files>src/ingest/imports/python.rs</files>
  <action>
    At line 95, replace:
    ```rust
    let imported_name = path.first().unwrap().clone();
    ```
    With:
    ```rust
    let imported_name = path.first().cloned().expect("path confirmed non-empty by is_empty() check");
    ```

    The key improvement: `.cloned()` returns `Option<String>` which is safer, and the expect message documents WHY this is safe (the is_empty check on line 93). Alternatively, restructure to avoid the unwrap entirely using match or if-let.

    Do NOT modify the test code unwrap() at line 310 - that will be handled in a separate plan.
  </action>
  <verify>cargo test --lib ingest::imports::python</verify>
  <done>Line 95 uses safe pattern (cloned() or match) with proper error handling</done>
</task>

<task type="auto">
  <name>Fix unwrap() on last() at line 201</name>
  <files>src/ingest/imports/python.rs</files>
  <action>
    At line 201, the code is inside a block guarded by `if name_path.is_empty() { continue; }` at line 190-192.

    Replace:
    ```rust
    imported_names.push(name_path.last().unwrap().clone());
    ```

    With safer pattern that explicitly handles the empty case:
    ```rust
    if let Some(name) = name_path.last().cloned() {
        imported_names.push(name);
    }
    ```

    This is cleaner because the continue statement above makes the unwrap technically safe, but using if-let makes the code more robust and explicit.
  </action>
  <verify>cargo test --lib ingest::imports::python</verify>
  <done>Line 201 uses if-let pattern instead of unwrap()</done>
</task>

</tasks>

<verification>
1. Read src/ingest/imports/python.rs and confirm:
   - No `.unwrap()` calls remain on `.first()` or `.last()` in production code
   - Lines 95 and 201 use safe alternatives (cloned(), if-let, or match)

2. Run tests to ensure no regression:
   ```bash
   cargo test --lib ingest::imports::python
   ```

3. Verify no new unwrap() calls were introduced elsewhere in the file.
</verification>

<success_criteria>
- No unwrap() on first() or last() in python.rs production code (lines 1-298)
- All existing tests pass: cargo test --lib ingest::imports::python
- Code still functions identically, just with safer error handling
</success_criteria>

<output>
After completion, create `.planning/phases/19-critical-error-handling/19-01-SUMMARY.md` with:
- Changes made (specific line numbers and patterns)
- Test results
- Any unexpected issues encountered
</output>
