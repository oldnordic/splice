---
phase: 27-code-cleanup
plan: 04
type: execute
wave: 3
depends_on: ["27-01", "27-02", "27-03"]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Codebase compiles successfully with cargo check"
    - "All tests pass (cargo test)"
    - "No clippy warnings specific to dead code"
    - "Phase 27 success criteria met"
  artifacts:
    - path: "src/ingest/mod.rs"
      provides: "Clean file without dead code"
    - path: ".planning/codebase/ARCHITECTURE.md"
      provides: "Updated documentation"
    - path: "CHANGELOG.md"
      provides: "Removal documented"
  key_links:
    - from: "All validation commands"
      to: "Success criteria"
      via: "Verification process"
      pattern: "cargo check && cargo test"
---

<objective>
Final validation of the code cleanup phase. Run comprehensive checks to ensure the codebase compiles, all tests pass, and the phase success criteria are fully met.

**Purpose**: Gate-keeping verification that the cleanup is complete and nothing is broken.

**Output**: Clean test run and confirmation that Phase 27 is complete.
</objective>

<execution_context>
@/home/feanor/.claude/get-shit-done/workflows/execute-plan.md
@/home/feanor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-code-cleanup/27-CONTEXT.md
@.planning/phases/27-code-cleanup/27-RESEARCH.md

# Phase Success Criteria
From ROADMAP.md:
1. Dead `Ingestor` struct stub removed from src/ingest/mod.rs
2. All dead code imports and unused dependencies cleaned
3. Documentation updated to reflect current architecture (no references to dead code)
4. Codebase compiles and all tests pass after cleanup
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify code compiles cleanly</name>
  <files>src/</files>
  <action>
    Run comprehensive compilation check:

    ```bash
    cargo check --verbose 2>&1 | tee /tmp/cargo_check.log
    ```

    Check for:
    - Successful compilation (exit code 0)
    - No warnings about unused imports from src/
    - No missing type errors
    - No broken imports after Ingestor removal

    If compilation fails:
    1. Capture the error
    2. Identify the root cause
    3. Fix and re-run

    Document the compilation result in the execution log.
  </action>
  <verify>cargo check exits with code 0, no src/ warnings</verify>
  <done>Codebase compiles successfully</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite</name>
  <files>tests/ src/</files>
  <action>
    Run the complete test suite to ensure nothing broke:

    ```bash
    cargo test --verbose 2>&1 | tee /tmp/cargo_test.log
    ```

    Check for:
    - All tests pass
    - No test failures related to removed code
    - Test count is similar to before cleanup (expected: same or fewer if dead tests were removed)

    Capture test summary:
    - Total tests run
    - Tests passed
    - Tests failed (should be 0)
    - Execution time

    If any tests fail, investigate whether they were related to the removed code.
  </action>
  <verify>All tests pass, zero failures</verify>
  <done>Full test suite passes</done>
</task>

<task type="auto">
  <name>Task 3: Run clippy for additional checks</name>
  <files>src/</files>
  <action>
    Run clippy to check for any remaining issues:

    ```bash
    cargo clippy -- -D warnings 2>&1 | tee /tmp/cargo_clippy.log
    ```

    Look for:
    - Dead code warnings (should be none after cleanup)
    - Unused imports (should be none after plan 27-02)
    - Any new warnings introduced by the changes

    Document any clippy warnings found. If warnings are specific to the removed code, that's expected (clippy may flag references that no longer exist).
  </action>
  <verify>No new clippy warnings related to removed code</verify>
  <done>Clippy validation complete</done>
</task>

<task type="auto">
  <name>Task 4: Verify all phase success criteria</name>
  <files>.planning/phases/27-code-cleanup/</files>
  <action>
    Verify each success criterion from ROADMAP.md:

    1. **Dead Ingestor struct removed:**
       ```bash
       grep -n "struct Ingestor" src/ingest/mod.rs
       ```
       Expected: No results

    2. **Dead code imports cleaned:**
       ```bash
       cargo check 2>&1 | grep -i "unused.*import" || echo "No unused imports"
       ```
       Expected: No unused import warnings from src/

    3. **Documentation updated:**
       ```bash
       grep "Ingestor" .planning/codebase/ARCHITECTURE.md | grep -v "removed in v2.2.4"
       ```
       Expected: Only removal notes, no active architecture mentions

    4. **Codebase compiles and tests pass:**
       From tasks 1 and 2 above.

    Create a checklist result document showing each criterion met.
  </action>
  <verify>All 4 success criteria verified as TRUE</verify>
  <done>Phase 27 success criteria confirmed</done>
</task>

</tasks>

<verification>
After completing all tasks:

1. Create a final validation summary showing:
   - Cargo check: PASSED / FAILED
   - Cargo test: X/Y tests passed
   - Clippy: Clean / X warnings
   - Success criteria: 4/4 verified

2. If any checks fail, document the issue and determine if rollback is needed.

3. All logs should be preserved in /tmp/ for reference.
</verification>

<success_criteria>
1. cargo check succeeds (exit code 0)
2. All tests pass (0 failures)
3. No unexpected clippy warnings
4. All 4 phase success criteria verified as TRUE
</success_criteria>

<output>
After completion, create `.planning/phases/27-code-cleanup/27-04-SUMMARY.md` with:
- Full validation results
- Test pass/fail summary
- Success criteria checklist
- Phase completion status
</output>
