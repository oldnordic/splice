---
phase: 27-code-cleanup
plan: 03
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - .planning/codebase/ARCHITECTURE.md
  - CHANGELOG.md
  - docs/TODO_MULTI_LANG.md
  - docs/EXECUTIVE_SUMMARY.md
  - docs/MULTI_LANGUAGE_V2.md
autonomous: true

must_haves:
  truths:
    - "ARCHITECTURE.md no longer references Ingestor struct"
    - "CHANGELOG.md has entry documenting the removal"
    - "TODO items about Ingestor removal are closed"
    - "Documentation correctly directs users to MagellanIngestor"
  artifacts:
    - path: ".planning/codebase/ARCHITECTURE.md"
      provides: "Updated architecture without Ingestor references"
    - path: "CHANGELOG.md"
      provides: "Record of removed dead code"
    - path: "docs/TODO_MULTI_LANG.md"
      provides: "TODO items closed with completion notes"
  key_links:
    - from: ".planning/codebase/ARCHITECTURE.md"
      to: "src/ingest/magellan.rs"
      via: "Updated documentation references"
      pattern: "MagellanIngestor"
---

<objective>
Update documentation to remove references to the deleted `Ingestor` struct and record the removal in the changelog. Documentation should accurately reflect the current architecture where `MagellanIngestor` and `extract_symbols()` are the correct ingestion APIs.

**Purpose**: Keep documentation synchronized with code reality and provide transparency about what was removed.

**Output**: Updated documentation files with no references to the dead `Ingestor` struct.
</objective>

<execution_context>
@/home/feanor/.claude/get-shit-done/workflows/execute-plan.md
@/home/feanor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-code-cleanup/27-CONTEXT.md
@.planning/phases/27-code-cleanup/27-RESEARCH.md

# Files That Reference Dead Code

From RESEARCH.md:
- `.planning/codebase/ARCHITECTURE.md` — Line 21: Remove "Ingestor: Main orchestrator..." entry
- `CHANGELOG.md` — Add entry documenting removal
- `docs/TODO_MULTI_LANG.md` — Item "Remove unused Ingestor.graph field" can be closed
- `docs/EXECUTIVE_SUMMARY.md` — Mentions removal as TODO
- `docs/MULTI_LANGUAGE_V2.md` — Mentions removal as TODO
- `docs/CONTEXT.md` — Already marks it as "abandoned design" (update to note removal complete)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ARCHITECTURE.md to remove Ingestor references</name>
  <files>.planning/codebase/ARCHITECTURE.md</files>
  <action>
    Update `.planning/codebase/ARCHITECTURE.md`:

    1. Remove line 21:
       ```
       - **Ingestor:** Main orchestrator for parsing source files
       ```

    2. Update the Ingest Layer section (lines 18-26) to reflect current architecture:
       - Remove `Ingestor` from the component list
       - Add note about `MagellanIngestor` being the correct API

    Updated section should read:
    ```
    ### Ingest Layer (`src/ingest/`)
    **Purpose:** Filesystem → AST → SQLiteGraph ingestion pipeline
    **Location:** `/home/feanor/Projects/splice/src/ingest/`
    - **Language Modules:** Individual parsers for each language (rust.rs, python.rs, etc.)
    - **Imports:** Cross-language import extraction and resolution
    - **Detection:** Automatic language detection from file extensions
    - **Dispatch:** Coordinated extraction across multiple languages
    - **Magellan Integration:** `MagellanIngestor` for database-backed indexing (preferred API)

    Note: The original `Ingestor` struct was removed in v2.2.4 as dead code.
    ```

    3. Update "Ingestion Flow" section (line 78) to remove `Ingestor` reference:
       Change "File Detection: `Ingestor` detects language..."
       To "File Detection: Language detected via file extension using `detect_language()`..."
  </action>
  <verify>No "Ingestor" references remain in ARCHITECTURE.md (except in notes about removal)</verify>
  <done>ARCHITECTURE.md reflects current architecture</done>
</task>

<task type="auto">
  <name>Task 2: Add CHANGELOG.md entry for dead code removal</name>
  <files>CHANGELOG.md</files>
  <action>
    Add a new version entry to `CHANGELOG.md` documenting the removal.

    Insert after the last entry (after [2.2.3] section):

    ```markdown
    ## [2.2.4] - 2026-02-XX

    ### Removed

    **Dead Code Cleanup**
    - Removed unused `Ingestor` struct from `src/ingest/mod.rs`
      This was an abandoned design from early development.
      Use `MagellanIngestor` or `extract_symbols()` instead.

    ### Changed

    **Documentation**
    - Updated ARCHITECTURE.md to remove Ingestor references
    - Closed TODO items related to Ingestor cleanup

    **Migration:** If you were using the `Ingestor` struct (unlikely, as it was never implemented),
    switch to:
    - `MagellanIngestor` for database-backed indexing
    - `extract_symbols()` for direct symbol extraction
    ```

    Use the current date (2026-02-04) or leave as placeholder for release date.
  </action>
  <verify>CHANGELOG.md has new version section with removal details</verify>
  <done>CHANGELOG.md documents the dead code removal</done>
</task>

<task type="auto">
  <name>Task 3: Close TODO items in documentation</name>
  <files>docs/TODO_MULTI_LANG.md
docs/EXECUTIVE_SUMMARY.md
docs/MULTI_LANGUAGE_V2.md
docs/CONTEXT.md</files>
  <action>
    Update or close TODO items related to Ingestor cleanup:

    1. In `docs/TODO_MULTI_LANG.md`:
       Change "- [ ] Remove unused `Ingestor.graph` field"
       To "- [x] Remove unused `Ingestor.graph` field (removed in v2.2.4)"

    2. In `docs/EXECUTIVE_SUMMARY.md`:
       Update or remove the TODO entry about removing `Ingestor.graph` field.

    3. In `docs/MULTI_LANGUAGE_V2.md`:
       Change "- [ ] Remove dead `Ingestor.graph` field"
       To "- [x] Remove dead `Ingestor.graph` field (removed in v2.2.4)"

    4. In `docs/CONTEXT.md`:
       Update the "ABANDONED DESIGN" section to note removal is complete:
       Add note: "**Status:** Removed in v2.2.4"
  </action>
  <verify>TODO items are marked complete with version reference</verify>
  <done>Documentation TODOs updated to reflect completion</done>
</task>

</tasks>

<verification>
After completing all tasks:

1. Verify ARCHITECTURE.md:
   ```bash
   grep -n "Ingestor" .planning/codebase/ARCHITECTURE.md
   ```
   Expected: Only references to removal in notes, no active architecture mentions

2. Verify CHANGELOG.md:
   ```bash
   grep -A 5 "2.2.4" CHANGELOG.md
   ```
   Expected: New section documenting removal

3. Verify TODO items:
   ```bash
   grep "Ingestor" docs/*.md | grep "\[x\]"
   ```
   Expected: TODO items marked complete
</verification>

<success_criteria>
1. ARCHITECTURE.md no longer describes Ingestor as an active component
2. CHANGELOG.md documents the removal with migration guidance
3. TODO items are marked complete
4. Documentation correctly points to MagellanIngestor as the replacement
</success_criteria>

<output>
After completion, create `.planning/phases/27-code-cleanup/27-03-SUMMARY.md` with:
- List of documentation files updated
- Changelog entry added
- TODO items closed
</output>
