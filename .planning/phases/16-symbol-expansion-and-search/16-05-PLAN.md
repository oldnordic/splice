---
phase: 16-symbol-expansion-and-search
plan: 05
type: execute
wave: 3
depends_on: [16-01, 16-02, 16-03, 16-04]
files_modified:
  - tests/expansion_tests.rs
  - src/lib.rs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Expansion works consistently across all 7 supported languages"
    - "Each language has at least 3 test cases covering different symbol types"
    - "Tests verify progressive expansion (level 0, 1, 2)"
    - "Tests verify doc comment inclusion"
  artifacts:
    - path: "tests/expansion_tests.rs"
      provides: "Comprehensive expansion tests for all 7 languages"
      min_lines: 400
    - path: "src/lib.rs"
      provides: "Test module declaration"
      contains: "#[cfg(test)] mod expansion_tests;"
  key_links:
    - from: "tests/expansion_tests.rs"
      to: "expand_symbol()"
      via: "direct function calls"
      pattern: "splice::expand::expand_symbol"
---

<objective>
Test expansion across all 7 languages for consistency.

Purpose: Ensure symbol expansion works correctly across all supported languages (Rust, Python, C, C++, Java, JavaScript, TypeScript) with comprehensive test coverage for different symbol types and expansion levels.

Output: Comprehensive integration test suite in tests/expansion_tests.rs with 21+ tests (3+ per language) covering functions, classes/structs, and nested symbols.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md

@.planning/phases/16-symbol-expansion-and-search/16-RESEARCH.md

@src/expand/mod.rs
@src/expand/tree_walker.rs
@tests/context_flags_tests.rs
</context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/16-symbol-expansion-and-search/16-01-PLAN.md
@.planning/phases/16-symbol-expansion-and-search/16-03-PLAN.md
@.planning/phases/16-symbol-expansion-and-search/16-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create expansion_tests.rs with test fixtures</name>
  <files>tests/expansion_tests.rs</files>
  <action>
Create tests/expansion_tests.rs with:
1. Module imports and test helpers (tempfile, splice::expand::*)
2. Language-specific test fixtures (Rust, Python, C, C++, Java, JavaScript, TypeScript)
3. Helper function create_test_file(lang, content) -> (TempDir, PathBuf, usize)
4. Helper function verify_expansion(path, offset, level, expected_content)

Follow pattern from existing tests/context_flags_tests.rs for structure.
  </action>
  <verify>cargo check --quiet</verify>
  <done>Test module structure compiles with helpers and fixtures</done>
</task>

<task type="auto">
  <name>Task 2: Add Rust expansion tests (3 tests)</name>
  <files>tests/expansion_tests.rs</files>
  <action>
Add 3 Rust-specific tests:
1. test_rust_function_expansion - expands fn with signature and body
2. test_rust_struct_expansion - expands struct definition with fields
3. test_rust_method_to_impl_expansion - level 2 expands method to impl block

Include doc comments in test fixtures to verify doc inclusion from plan 16-04.
  </action>
  <verify>cargo test --quiet expansion_tests::rust</verify>
  <done>All 3 Rust expansion tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Add Python expansion tests (3 tests)</name>
  <files>tests/expansion_tests.rs</files>
  <action>
Add 3 Python-specific tests:
1. test_python_function_expansion - expands def with signature and body
2. test_python_class_expansion - expands class definition with methods
3. test_python_method_to_class_expansion - level 2 expands method to class body

Include docstrings (""") in test fixtures.
  </action>
  <verify>cargo test --quiet expansion_tests::python</verify>
  <done>All 3 Python expansion tests pass</done>
</task>

<task type="auto">
  <name>Task 4: Add C/C++ expansion tests (3 tests each)</name>
  <files>tests/expansion_tests.rs</files>
  <action>
Add 3 C-specific tests:
1. test_c_function_expansion - expands function with signature and body
2. test_c_struct_expansion - expands struct definition
3. test_c_expansion_with_comments - verifies /* */ and /// comment capture

Add 3 C++-specific tests:
1. test_cpp_class_expansion - expands class definition with members
2. test_cpp_method_expansion - expands method with signature
3. test_cpp_method_to_class_expansion - level 2 expands method to class
  </action>
  <verify>cargo test --quiet expansion_tests::cpp</verify>
  <done>All 6 C/C++ expansion tests pass</done>
</task>

<task type="auto">
  <name>Task 5: Add Java expansion tests (3 tests)</name>
  <files>tests/expansion_tests.rs</files>
  <action>
Add 3 Java-specific tests:
1. test_java_method_expansion - expands method with signature and body
2. test_java_class_expansion - expands class definition with fields and methods
3. test_java_method_to_class_expansion - level 2 expands method to class

Include Javadoc (/** */) in test fixtures.
  </action>
  <verify>cargo test --quiet expansion_tests::java</verify>
  <done>All 3 Java expansion tests pass</done>
</task>

<task type="auto">
  <name>Task 6: Add JavaScript/TypeScript expansion tests (3 tests each)</name>
  <files>tests/expansion_tests.rs</files>
  <action>
Add 3 JavaScript-specific tests:
1. test_js_function_expansion - expands function declaration
2. test_js_class_expansion - expands class definition
3. test_js_method_to_class_expansion - level 2 expands method to class

Add 3 TypeScript-specific tests:
1. test_ts_function_expansion - expands function with type annotations
2. test_ts_interface_expansion - expands interface definition
3. test_ts_method_to_interface_expansion - level 2 expands method to interface/class

Include JSDoc (/** */) in test fixtures.
  </action>
  <verify>cargo test --quiet expansion_tests::js</verify>
  <done>All 6 JavaScript/TypeScript expansion tests pass</done>
</task>

<task type="auto">
  <name>Task 7: Add progressive expansion level tests</name>
  <files>tests/expansion_tests.rs</files>
  <action>
Add cross-language tests for expansion levels:
1. test_level_0_no_expansion - all languages return original span
2. test_level_1_body_expansion - all languages return symbol body
3. test_level_2_containing_block - nested symbols expand to containing block
4. test_level_2_no_containing_block - top-level symbols stay at level 1

These tests verify the progressive expansion behavior from plan 16-03.
  </action>
  <verify>cargo test --quiet expansion_tests::progressive</verify>
  <done>All 4 progressive expansion tests pass</done>
</task>

</tasks>

<verification>
- [ ] `cargo check` passes with no errors
- [ ] `cargo test --quiet expansion_tests` passes all tests
- [ ] All 7 languages have at least 3 tests
- [ ] Tests cover expansion levels 0, 1, 2
- [ ] Tests verify doc comment inclusion
- [ ] Total test count >= 21 (3 per language minimum)
</verification>

<success_criteria>
1. tests/expansion_tests.rs created with 21+ tests
2. Each of 7 languages has 3+ tests covering different symbol types
3. Progressive expansion levels (0, 1, 2) verified
4. Doc comment inclusion verified for each language's doc style
5. All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/16-symbol-expansion-and-search/16-05-SUMMARY.md`
</output>
