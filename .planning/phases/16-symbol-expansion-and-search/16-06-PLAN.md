---
phase: 16-symbol-expansion-and-search
plan: 06
type: execute
wave: 3
depends_on: [16-01, 16-02, 16-03, 16-04]
files_modified:
  - tests/context_expansion_integration_tests.rs
  - src/main.rs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Context flags (-A/-B/-C) respect expanded symbol boundaries when used with --expand"
    - "Context is calculated from expanded span, not original offset"
    - "Tests verify interaction across all combinations"
  artifacts:
    - path: "tests/context_expansion_integration_tests.rs"
      provides: "Integration tests for context + expansion interaction"
      min_lines: 200
    - path: "src/main.rs"
      provides: "execute_get and execute_query with boundary recalculation"
      contains: "let expanded_start = expand"
  key_links:
    - from: "src/main.rs execute_get"
      to: "extract_context_asymmetric()"
      via: "call with expanded boundaries"
      pattern: "extract_context_asymmetric\\(path, expanded_start, expanded_end"
    - from: "tests/context_expansion_integration_tests.rs"
      to: "execute_get()"
      via: "integration test calling both features"
      pattern: "execute_get\\(&db, &file"
---

<objective>
Test context flags respect expanded symbol boundaries (CLI-14 from Phase 14).

Purpose: Verify that when --expand is used with -A/-B/-C context flags, the context is calculated from the expanded symbol boundaries, not the original byte offset. This ensures users get context around the full symbol, not just the original location.

Output: Integration tests verifying context flag + expansion flag interaction, with boundary recalculation in execute_get and execute_query.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md

@.planning/phases/16-symbol-expansion-and-search/16-RESEARCH.md

@src/main.rs
@src/context.rs
@src/expand/mod.rs
@tests/context_flags_tests.rs
</context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/14-context-flags/14-05-SUMMARY.md

@.planning/phases/16-symbol-expansion-and-search/16-02-PLAN.md
@.planning/phases/16-symbol-expansion-and-search/16-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update execute_get to recalculate boundaries with expansion</name>
  <files>src/main.rs</files>
  <action>
Modify execute_get to apply expansion before context extraction:
1. After initial span retrieval, check if expand is true
2. If expand && expand_level > 0, call splice::expand_symbol()
3. Replace (start, end) with expanded (expanded_start, expanded_end)
4. Call extract_context_asymmetric() with expanded boundaries
5. Include both original and expanded spans in JSON output

The key change: context is extracted from expanded span, not original span.
```rust
let (content_start, content_end) = if expand && expand_level > 0 {
    let level = match expand_level {
        0 => ExpansionLevel::None,
        1 => ExpansionLevel::Body,
        _ => ExpansionLevel::ContainingBlock,
    };
    splice::expand_symbol(&file, start, language, level)?
} else {
    (start, end)
};

// Context extraction uses expanded boundaries
let context = splice::extract_context_asymmetric(&file, content_start, content_end, context_before, context_after)?;
```
  </action>
  <verify>cargo check --quiet</verify>
  <done>execute_get applies expansion before context extraction</done>
</task>

<task type="auto">
  <name>Task 2: Update execute_query to recalculate boundaries with expansion</name>
  <files>src/main.rs</files>
  <action>
Modify execute_query similarly:
1. For each query result, check if expand is true
2. If expand && expand_level > 0, call splice::expand_symbol() per result
3. Replace result spans with expanded spans
4. Call extract_context_asymmetric() with expanded boundaries
5. Include expansion info in JSON output

Apply the same pattern as execute_get: context calculated from expanded span.
  </action>
  <verify>cargo check --quiet</verify>
  <done>execute_query applies expansion before context extraction</done>
</task>

<task type="auto">
  <name>Task 3: Create context+expansion integration tests</name>
  <files>tests/context_expansion_integration_tests.rs</files>
  <action>
Create tests/context_expansion_integration_tests.rs with:
1. Module imports and test helpers
2. Test fixtures for all 7 languages with symbols and context
3. Helper function to run splice get command with flags

Add tests:
1. test_context_with_expand_rust - verify context around expanded function
2. test_context_with_expand_python - verify context around expanded class
3. test_context_before_respects_expansion - -B lines before expanded symbol
4. test_context_after_respects_expansion - -A lines after expanded symbol
5. test_context_both_respects_expansion - -C lines around expanded symbol
6. test_context_no_expand_baseline - verify original behavior unchanged
7. test_context_expand_level_2 - context around containing block
8. test_json_output_has_both_spans - original and expanded spans in JSON

Each test creates a temp file, queries with flags, verifies context includes expanded symbol boundaries.
  </action>
  <verify>cargo test --quiet context_expansion_integration</verify>
  <done>All 8 context+expansion integration tests pass</done>
</task>

</tasks>

<verification>
- [ ] `cargo check` passes with no errors
- [ ] `cargo test --quiet context_expansion_integration` passes all tests
- [ ] Context calculated from expanded span when --expand used
- [ ] Original span preserved in JSON output
- [ ] All combinations of -A/-B/-C with --expand tested
- [ ] All 7 languages covered in tests
</verification>

<success_criteria>
1. execute_get and execute_query recalculate boundaries with expansion
2. Context extraction uses expanded boundaries
3. 8 integration tests verify interaction
4. JSON output includes both original and expanded spans
5. CLI-14 from Phase 14 requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/16-symbol-expansion-and-search/16-06-SUMMARY.md`
</output>
