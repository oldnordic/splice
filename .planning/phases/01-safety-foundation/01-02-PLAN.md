---
phase: 01-safety-foundation
plan: 02
type: execute
depends_on: ["01-01"]
files_modified: [src/plan/mod.rs, src/main.rs, src/patch/pattern.rs, src/patch/backup.rs, src/validate/gates.rs]
autonomous: true
---

<objective>
Replace unwrap() calls in core production paths with proper error handling.

Purpose: Fix critical paths (plan execution, CLI entry, patch operations, validation gates) where unwrap() could cause panics in production. These are high-priority because they affect user-facing operations.

Output: All unwrap() calls in core modules replaced with `?` operator and proper error context.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@.planning/phases/01-safety-foundation/01-01-PLAN.md
@src/error.rs

**Depends on Plan 01-01:**
- Uses audit document to identify exact unwrap() locations
- Uses error context helpers added in 01-01

**Files to fix (from audit):**
- src/plan/mod.rs: lines 204, 253, 262 (3 calls)
- src/main.rs: lines 196, 459 (2 calls)
- src/patch/pattern.rs: line 165 (1 call)
- src/patch/backup.rs: line 423 (1 call)
- src/validate/gates.rs: lines 93, 135, 177, 219, 262, 315 (6 calls)

**Replacement patterns:**
- `.parent().unwrap()` → `.parent().ok_or_else(|| SpliceError::Other(...))` or validate earlier
- `.to_str().unwrap()` in Command args → validate path is UTF-8 earlier, use `.to_str().map_err(|_| SpliceError::Other(...))?`
- `serde_json::from_str(...).unwrap()` → `serde_json::from_str(...).map_err(|e| SpliceError::InvalidPlanSchema { message: e.to_string() })?`
- `TempDir::new().unwrap()` in tests only - skip if in #[cfg(test)]
- Result types that already exist: use `?` operator

**Do NOT modify test code** - unwrap() in #[cfg(test)] is acceptable.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix src/plan/mod.rs unwrap() calls</name>
  <files>src/plan/mod.rs</files>
  <action>Fix unwrap() calls at lines 204, 253, 262:

Line 204: `let graph_db_path = file_path.parent().unwrap().join(".splice_graph.db");`
Replacement: Use `file_path.parent().ok_or_else(|| SpliceError::Other(format!("File has no parent: {}", file_path.display())))?`

Line 253: `let plan: Plan = serde_json::from_str(json).unwrap();`
Replacement: `serde_json::from_str(json).map_err(|e| SpliceError::InvalidPlanSchema { message: format!("Invalid plan JSON: {}", e) })?`

Line 262: `let result = serde_json::from_str::<Plan>(r#"{"steps": []}"#).unwrap();`
Replacement: This appears to be test/verification code. If it's checking empty plan handling, use `serde_json::from_str::<Plan>(r#"{"steps": []}"#).map_err(|_| SpliceError::InvalidPlanSchema { message: "Empty plan".into() })?` or restructure the validation approach.</action>
  <verify>cargo check passes
cargo test passes</verify>
  <done>No unwrap() calls remain in src/plan/mod.rs</done>
</task>

<task type="auto">
  <name>Task 2: Fix src/main.rs and src/patch/*.rs unwrap() calls</name>
  <files>src/main.rs, src/patch/pattern.rs, src/patch/backup.rs</files>
  <action>Fix unwrap() calls:

**src/main.rs:**
Line 196: Same pattern as plan/mod.rs line 204
Line 459: Same pattern as plan/mod.rs line 204

**src/patch/pattern.rs:**
Line 165: `let rope = ropey::Rope::from_reader(content.as_bytes()).unwrap();`
Replacement: `ropey::Rope::from_reader(content.as_bytes()).map_err(|e| SpliceError::Other(format!("Failed to create rope: {}", e)))?`

**src/patch/backup.rs:**
Line 423: `fs::create_dir_all(manifest_path.parent().unwrap())`
Replacement: Validate parent exists earlier or use `if let Some(parent) = manifest_path.parent() { fs::create_dir_all(parent)? } else { return Err(SpliceError::Other(...)); }`</action>
  <verify>cargo check passes
No unwrap() calls in these files (verify with grep)</verify>
  <done>All unwrap() calls replaced in src/main.rs, src/patch/pattern.rs, src/patch/backup.rs</done>
</task>

<task type="auto">
  <name>Task 3: Fix src/validate/gates.rs unwrap() calls</name>
  <files>src/validate/gates.rs</files>
  <action>Fix unwrap() calls at lines 93, 135, 177, 219, 262, 315:

These are all `.to_str().unwrap()` calls on Path arguments to Command.

Pattern: `.args(["-m", "py_compile", path.to_str().unwrap()])`

Replacement: Validate UTF-8 path before Command building:
```rust
let path_str = path.to_str()
    .ok_or_else(|| SpliceError::Other(format!("Invalid UTF-8 path: {}", path.display())))?;
```

Then use `path_str` in the .args() call.

Do this for all 6 occurrences. Each is a different validation gate (Python, C, C++, TypeScript, JavaScript).</action>
  <verify>cargo check passes
No unwrap() calls remain in src/validate/gates.rs</verify>
  <done>All validation gate unwrap() calls replaced with proper error handling</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo check passes without errors
- [ ] cargo test passes
- [ ] grep confirms no unwrap() in modified files (excluding comments and test code)
- [ ] All replacements follow error handling patterns
</verification>

<success_criteria>
- All unwrap() calls in core modules replaced with proper error handling
- Code compiles without warnings
- Tests still pass
- Error messages provide useful context for debugging
</success_criteria>

<output>
After completion, create `.planning/phases/01-safety-foundation/01-02-SUMMARY.md`

Include in SUMMARY.md:
- Number of unwrap() calls replaced
- Any unexpected issues encountered
- Verification that tests still pass
