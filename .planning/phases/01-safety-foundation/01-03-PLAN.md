---
phase: 01-safety-foundation
plan: 03
type: execute
depends_on: ["01-01"]
files_modified: [src/ingest/dispatch.rs, src/ingest/magellan.rs, src/ingest/typescript.rs, src/ingest/java.rs, src/ingest/javascript.rs, src/ingest/python.rs, src/ingest/cpp.rs, src/ingest/imports/*.rs]
autonomous: true
---

<objective>
Replace unwrap() calls in language ingest and import resolution modules.

Purpose: Fix unwrap() calls in language-specific symbol extraction and import resolution. These modules handle the core parsing logic for 7 languages. Parallel to Plan 01-02 (different files).

Output: All unwrap() calls in ingest modules replaced with proper error handling.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@.planning/phases/01-safety-foundation/01-01-PLAN.md
@src/error.rs

**Depends on Plan 01-01:**
- Uses audit document to identify exact unwrap() locations
- Uses error context helpers added in 01-01

**Files to fix (from audit):**
- src/ingest/dispatch.rs: 10 calls (lines 144, 157, 170, 183, 196, 209, 230, 242)
- src/ingest/magellan.rs: 3 calls (test code - may skip)
- src/ingest/typescript.rs: 10 calls
- src/ingest/java.rs: 8 calls
- src/ingest/javascript.rs: 5 calls
- src/ingest/python.rs: 1 call (line 272)
- src/ingest/cpp.rs: 1 call (line 508)
- src/ingest/imports/typescript.rs: 8 calls
- src/ingest/imports/java.rs: 6 calls
- src/ingest/imports/cpp.rs: 2 calls
- src/ingest/imports/javascript.rs: 6 calls
- src/ingest/imports/python.rs: 2 calls (lines 94, 199 - check if safe context)

**Pattern in ingest modules:**
Most are `let symbols = result.unwrap();` where result is from language-specific extract function.

**Replacement approach:**
1. Change extract function signatures to return `SpliceError` instead of panicking
2. Use `?` operator: `let symbols = result?;`
3. Add context if needed: `let symbols = result.map_err(|e| SpliceError::Parse { file, message: e.to_string() })?;`

**Do NOT modify:**
- Test code (#[cfg(test)] sections)
- src/ingest/imports/python.rs lines 94, 199 if they're truly safe (after is_some() check)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix src/ingest/dispatch.rs unwrap() calls</name>
  <files>src/ingest/dispatch.rs</files>
  <action>Fix 10 unwrap() calls at lines 144, 157, 170, 183, 196, 209, 230, 242.

Pattern: Each line is `let symbols = result.unwrap();` where result is from a language-specific ingest function.

The dispatch.rs calls functions like extract_rust_symbols(), extract_python_symbols(), etc.

Replace with: `let symbols = result.map_err(|e| SpliceError::Other(format!("Ingest failed: {}", e)))?;`

Alternative: If the called functions already return Result, just use `?`: `let symbols = result?;`

Check what the underlying functions return and adjust accordingly. The goal is to propagate errors properly rather than panic.</action>
  <verify>cargo check passes
No unwrap() in src/ingest/dispatch.rs (excluding tests)</verify>
  <done>All dispatch unwrap() calls replaced</done>
</task>

<task type="auto">
  <name>Task 2: Fix src/ingest/*.rs unwrap() calls</name>
  <files>src/ingest/typescript.rs, src/ingest/java.rs, src/ingest/javascript.rs, src/ingest/python.rs, src/ingest/cpp.rs, src/ingest/magellan.rs</files>
  <action>Fix unwrap() calls in language ingest modules:

**src/ingest/typescript.rs:** 10 calls at lines 384, 396, 408, 420, 432, 444, 457, 469, 481, 493

**src/ingest/java.rs:** 8 calls at lines 322, 334, 349, 362, 376, 388, 402, 413

**src/ingest/javascript.rs:** 5 calls at lines 356, 368, 381, 393, 405, 417

**src/ingest/python.rs:** 1 call at line 272

**src/ingest/cpp.rs:** 1 call at line 508

**src/ingest/magellan.rs:** Skip if in test code (#[cfg(test)])

Pattern is similar to dispatch.rs: `let symbols = result.unwrap();` where result comes from parsing.

Replace with: `let symbols = result.map_err(|e| SpliceError::Parse { file: file.clone(), message: e.to_string() })?;`

Or use `?` if the function already returns SpliceError::Result type.</action>
  <verify>cargo check passes
grep shows no unwrap() in these files (excluding tests)</verify>
  <done>All language ingest module unwrap() calls replaced</done>
</task>

<task type="auto">
  <name>Task 3: Fix src/ingest/imports/*.rs unwrap() calls</name>
  <files>src/ingest/imports/typescript.rs, src/ingest/imports/java.rs, src/ingest/imports/cpp.rs, src/ingest/imports/javascript.rs, src/ingest/imports/python.rs</files>
  <action>Fix unwrap() calls in import resolution modules:

**src/ingest/imports/typescript.rs:** 8 calls at lines 303, 316, 328, 341, 353, 366, 378, 388, 400

**src/ingest/imports/java.rs:** 6 calls at lines 143, 155, 167, 179, 191, 203

**src/ingest/imports/cpp.rs:** 2 calls at lines 139, 151

**src/ingest/imports/javascript.rs:** 6 calls at lines 255, 268, 280, 293, 305, 318, 328

**src/ingest/imports/python.rs:** 2 calls at lines 94, 199 - CHECK IF SAFE
These are `path.first().unwrap()` calls - verify they're after is_some() check.
If safe (validated), add comment explaining why.
If not safe, add proper error handling.

Pattern: Similar to ingest modules - `let imports = result.unwrap();`

Replace with: `let imports = result.map_err(|e| SpliceError::Other(format!("Import resolution failed: {}", e)))?;`

Or use `?` if Result type already compatible.</action>
  <verify>cargo check passes
All unsafe unwrap() calls replaced, safe ones documented</verify>
  <done>All import module unwrap() calls handled</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo check passes without errors
- [ ] cargo test passes
- [ ] grep confirms no unsafe unwrap() in ingest modules
- [ ] Safe unwrap() calls have explanatory comments
</verification>

<success_criteria>
- All unwrap() calls in language ingest modules fixed
- All unwrap() calls in import resolution modules fixed
- Code compiles and tests pass
- Error messages provide context for debugging
</success_criteria>

<output>
After completion, create `.planning/phases/01-safety-foundation/01-03-SUMMARY.md`

Include in SUMMARY.md:
- Number of unwrap() calls replaced across ingest modules
- Any issues with tree-sitter or language-specific parsing
- Verification that all 7 languages still work correctly
