---
phase: 01-safety-foundation
plan: 01
type: execute
depends_on: []
files_modified: [src/error.rs, .planning/phases/01-safety-foundation/unwrap-audit.md]
autonomous: true
---

<objective>
Establish error handling infrastructure and complete unwrap() audit.

Purpose: Create helper functions for error context before fixing unwrap() calls, and catalog all unsafe calls for systematic replacement. This foundation enables subsequent plans to efficiently replace unwrap() with proper error handling.

Output: Error context helpers in src/error.rs, complete audit document listing all 83 unwrap() calls categorized by safety.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@src/error.rs

**Key Context from CONCERNS.md:**
- unwrap() calls at: src/plan/mod.rs:253,262, src/graph/magellan_integration.rs:68,187, src/validate/gates.rs:93,135,177
- Fix approach: Replace with `?` operator and context
- SpliceError already has comprehensive variants for different error types

**Rust Error Handling Pattern:**
- Use `?` operator for propagating errors
- Use `.context()` or `.map_err()` to add context to errors
- SpliceError::Other(String) exists for generic errors
- Use `SpliceError::Io { path, source }` for file operations

**Categorization for audit:**
1. **Test code** - unwrap() in #[cfg(test)] is acceptable
2. **Safe context** - unwrap() after is_some() check
3. **Production paths** - requires replacement with proper error handling
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add error context helpers to src/error.rs</name>
  <files>src/error.rs</files>
  <action>Add convenience methods to SpliceError for adding context:

1. Add a `.with_context()` method that works like anyhow::context
2. Add helper for Path operations: `pub fn with_path(self, path: &Path) -> Self`
3. Ensure all variants support context propagation

Implementation: Add these as impl methods on SpliceError. Follow existing patterns in the file. Use the existing Diagnostic pattern for consistency.

Do NOT add new error variants - the existing variants cover all cases.

Do NOT modify existing methods - only add new helper methods at the end of the impl block.</action>
  <verify>cargo check passes without errors</verify>
  <done>New context helper methods available on SpliceError</done>
</task>

<task type="auto">
  <name>Task 2: Audit and catalog all unwrap() calls</name>
  <files>.planning/phases/01-safety-foundation/unwrap-audit.md</files>
  <action>Create comprehensive audit document by running grep to find all unwrap() calls.

Run: `grep -rn '\.unwrap()' src/` to get the current list (83 total as of 2026-01-17).

Categorize each call into:
1. **SAFE - Test code**: In #[cfg(test)] modules or test functions
2. **SAFE - Validated**: Preceded by is_some() or is_ok() check
3. **UNSAFE - Production**: Requires replacement

For each unsafe call, document:
- File path and line number
- Current code snippet
- Suggested replacement pattern (e.g., `.unwrap()` → `?` with context)
- Priority (high if in critical path like validation, medium otherwise)

Format as Markdown with sections for each category.</action>
  <verify>Audit file created at .planning/phases/01-safety-foundation/unwrap-audit.md
Count total unwrap() calls and verify categorization sums to total</verify>
  <done>Complete audit document with all unwrap() calls categorized</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo check passes without errors
- [ ] Audit document exists and categorizes all unwrap() calls
- [ ] New context helper methods are available
- [ ] Total count matches grep results (83 as of audit)
</verification>

<success_criteria>
- Error context helpers added to src/error.rs
- Complete audit of all 83 unwrap() calls categorized by safety
- Audit document provides roadmap for Plans 01-02 and 01-03
- No compilation errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/01-safety-foundation/01-01-SUMMARY.md`

Include in SUMMARY.md:
- Total unwrap() calls found
- Breakdown by category (test code, safe context, production paths)
- Priority ranking for production path fixes
- Any unexpected patterns discovered
