---
phase: 26-integration-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [tests/cli_output_tests.rs]
autonomous: true

must_haves:
  truths:
    - "Export command produces valid JSON output with schema_version field"
    - "Export command produces valid JSONL output with type-tagged records"
    - "Export command produces valid CSV output with section headers"
    - "All three export formats can be written to file via --file flag"
    - "All three export formats can be written to stdout (no --file flag)"
  artifacts:
    - path: "tests/cli_output_tests.rs"
      provides: "Export format validation tests"
      min_lines: 350
  key_links:
    - from: "tests/cli_output_tests.rs"
      to: "execute_export function"
      via: "Command subprocess testing"
      pattern: "Command::new.*arg.*export"
    - from: "tests/cli_output_tests.rs"
      to: "serde_json::from_str"
      via: "JSON/JSONL validation"
      pattern: "serde_json::from_str"
    - from: "tests/cli_output_tests.rs"
      to: "std::fs::read_to_string"
      via: "File output validation"
      pattern: "std::fs::read_to_string.*export"
---

<objective>
Export format validation tests - comprehensive testing of JSON, JSONL, and CSV export formats

Purpose: Validate that the export command produces correctly formatted output in all three formats (json, jsonl, csv) with proper schema validation
Output: Additional export format tests in tests/cli_output_tests.rs (extending existing 5 tests from Phase 25)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/26-integration-testing/26-RESEARCH.md

# Reference existing export tests (Phase 25-04)
@.planning/phases/25-export-command-and-error-mapping/25-04-SUMMARY.md

# Reference test patterns
@tests/cli_output_tests.rs
@tests/cli_tests.rs
@src/cli/mod.rs
@src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add comprehensive JSON schema validation test</name>
  <files>tests/cli_output_tests.rs</files>
  <action>
Add test `test_export_json_schema_validation` that extends the existing test_export_json_format:

1. Create TempDir with multiple indexed files (Rust and Python)
2. Run `splice export --db <db_path> --format json --file export.json`
3. Read export.json and validate complete schema:
   - schema_version == EXPORT_SCHEMA_VERSION ("1.0.0")
   - timestamp is a valid ISO 8601 string
   - db_path matches test database
   - data.files is array with >= 2 elements
   - data.symbols is array with >= 2 elements
   - data.references is array (may be empty)
   - data.calls is array (may be empty)
4. For each symbol in data.symbols, validate required fields:
   - symbol_id (16-char hex) or null
   - name (non-empty string)
   - kind (one of: fn, method, struct, class, enum, interface, module, etc.)
   - file_path (absolute path)
   - byte_start < byte_end
   - start_line >= 1
   - end_line >= start_line
5. Validate Magellan field naming (start_line not line_start)

This is a more comprehensive version of the existing test_export_json_format.
  </action>
  <verify>cargo test test_export_json_schema_validation passes</verify>
  <done>JSON export produces valid schema with all required fields and Magellan-compatible naming</done>
</task>

<task type="auto">
  <name>Task 2: Add JSONL record type validation test</name>
  <files>tests/cli_output_tests.rs</files>
  <action>
Add test `test_export_jsonl_record_types` that extends the existing test_export_jsonl_format:

1. Create TempDir with indexed code containing files, symbols, and references
2. Run `splice export --db <db_path> --format jsonl --file export.jsonl`
3. Read export.jsonl line by line
4. Validate record types:
   - First line should be type: "header" with schema_version
   - Subsequent lines are type: "file" (path, hash fields)
   - Additional lines are type: "symbol" (name, kind, file_path fields)
   - Additional lines are type: "reference" (from_symbol, to_symbol fields)
   - Additional lines are type: "call" (caller, callee, call_site fields)
5. Verify all lines are valid JSON (serde_json::from_str succeeds)
6. Verify all non-header lines have required fields for their type

This extends the existing test_export_jsonl_format to validate all record types.
  </action>
  <verify>cargo test test_export_jsonl_record_types passes</verify>
  <done>JSONL export produces type-tagged records with all required fields per type</done>
</task>

<task type="auto">
  <name>Task 3: Add CSV section structure validation test</name>
  <files>tests/cli_output_tests.rs</files>
  <action>
Add test `test_export_csv_section_structure` that extends the existing test_export_csv_format:

1. Create TempDir with multiple indexed files
2. Run `splice export --db <db_path> --format csv --file export.csv`
3. Read export.csv and validate structure:
4. Validate section headers:
   - File starts with "# Files" section header
   - Contains "# Symbols" section header
   - May contain "# References" section header
   - May contain "# Calls" section header
5. Validate column headers after each section:
   - # Files section has "path","hash" headers
   - # Symbols section has "symbol_id","name","kind","file_path","byte_start","byte_end" headers
6. Validate data rows match column count
7. Validate no empty rows between sections

This extends the existing test_export_csv_format to validate full structure.
  </action>
  <verify>cargo test test_export_csv_section_structure passes</verify>
  <done>CSV export produces properly sectioned output with headers and matching data rows</done>
</task>

<task type="auto">
  <name>Task 4: Add export error handling tests</name>
  <files>tests/cli_output_tests.rs</files>
  <action>
Add test `test_export_error_handling`:

1. Test database error (nonexistent database):
   - Run `splice export --db /nonexistent.db --format json`
   - Verify exit code 3 (database error)
2. Test invalid format (if CLI validates format):
   - Run `splice export --db <db_path> --format invalid`
   - Verify exit code 2 (usage error) or appropriate error
3. Test file write permission error:
   - Create TempDir, create read-only subdirectory
   - Run `splice export --db <db_path> --file /readonly/path/export.json`
   - Verify exit code 1 (error) with appropriate error message
4. Validate stderr contains error JSON with error_code field

For each error case, parse stderr and verify structured error response.
  </action>
  <verify>cargo test test_export_error_handling passes</verify>
  <done>Export command returns appropriate exit codes for all error conditions</done>
</task>

</tasks>

<verification>
Run all export format tests:
```bash
cargo test test_export_
```

Expected: All new tests pass, plus existing 5 export tests from Phase 25 still pass.
</verification>

<success_criteria>
- [ ] test_export_json_schema_validation passes
- [ ] test_export_jsonl_record_types passes
- [ ] test_export_csv_section_structure passes
- [ ] test_export_error_handling passes
- [ ] All existing export tests still pass (test_export_json_format, test_export_jsonl_format, test_export_csv_format, test_export_defaults_to_json, test_export_stdout_output)
- [ ] No regressions in other tests
</success_criteria>

<output>
After completion, create `.planning/phases/26-integration-testing/26-02-SUMMARY.md`
</output>
