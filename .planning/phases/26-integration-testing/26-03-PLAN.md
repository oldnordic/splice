---
phase: 26-integration-testing
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [tests/cli_tests.rs]
autonomous: true

must_haves:
  truths:
    - "Magellan errors from database operations are mapped to SPL-E091 error code"
    - "Error responses include structured error_code with code, severity, and message fields"
    - "Original Magellan error is preserved in error chain (via anyhow::Error source)"
    - "CLI exit codes match SpliceErrorCode values for all error types"
  artifacts:
    - path: "tests/cli_tests.rs"
      provides: "Error code mapping integration tests"
      min_lines: 200
  key_links:
    - from: "tests/cli_tests.rs"
      to: "SpliceError::Magellan variant"
      via: "Magellan error injection tests"
      pattern: "SpliceError::Magellan"
    - from: "tests/cli_tests.rs"
      to: "SPL-E091 error code"
      via: "Error code validation in JSON output"
      pattern: "SPL-E091"
    - from: "tests/cli_tests.rs"
      to: "SpliceExitCode::from_error()"
      via: "Exit code validation"
      pattern: "exit_code.*3.*database"
---

<objective>
Error code mapping tests - validate that Magellan errors are correctly mapped to Splice error codes

Purpose: Verify that Magellan database errors propagate through SpliceError::Magellan and map to SPL-E091 with proper exit codes
Output: 4 new integration tests in tests/cli_tests.rs covering Magellan error mapping scenarios
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/26-integration-testing/26-RESEARCH.md

# Reference Phase 25-02 error mapping implementation
@.planning/phases/25-export-command-and-error-mapping/25-02-SUMMARY.md

# Reference error codes and SpliceError definition
@src/error_codes.rs
@src/error.rs
@src/main.rs

# Reference test patterns for error validation
@tests/llm_consumption_tests.rs
@tests/cli_tests.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Magellan database error to SPL-E091 test</name>
  <files>tests/cli_tests.rs</files>
  <action>
Add test `test_magellan_database_error_maps_to_spl_e091`:

1. Create TempDir
2. Run `splice status --db /nonexistent/path/to/database.db`
   (This triggers MagellanGraph::open() error which becomes SpliceError::Magellan)
3. Verify exit code is 3 (database error per SpliceExitCode)
4. Parse stderr with extract_json_from_stdout() or read stderr directly
5. Validate error response structure:
   - status == "error"
   - error.error_code.code == "SPL-E091"
   - error.error_code.severity == "error"
   - error.error_code.hint contains "Magellan" or "database"
6. Verify error.message contains database path or "open" or "Magellan"

Reference: src/error_codes.rs for SPL-E091 definition and src/error.rs for SpliceError::Magellan variant.
  </action>
  <verify>cargo test test_magellan_database_error_maps_to_spl_e091 passes</verify>
  <done>Magellan database errors map to SPL-E091 with correct exit code 3</done>
</task>

<task type="auto">
  <name>Task 2: Add query command Magellan error test</name>
  <files>tests/cli_tests.rs</files>
  <action>
Add test `test_magellan_query_error_preserves_context`:

1. Create TempDir and db_path
2. Create valid database with one indexed file
3. Corrupt the database by writing invalid data (or use read-only directory trick)
4. Run `splice query --db <db_path> --label rust`
5. Verify exit code is 1 (error) or 3 (database error depending on error type)
6. Parse stderr JSON
7. Validate:
   - error.error_code.code == "SPL-E091" (if Magellan error) or appropriate code
   - error.message contains query context ("query", "labels", "rust")
8. Verify original error is preserved (message contains Magellan-specific details)

The key validation is that Magellan errors include context about which operation failed.
  </action>
  <verify>cargo test test_magellan_query_error_preserves_context passes</verify>
  <done>Magellan errors from query operations preserve operation context in error message</done>
</task>

<task type="auto">
  <name>Task 3: Add symbol not found error code test</name>
  <files>tests/cli_tests.rs</files>
  <action>
Add test `test_symbol_not_found_error_code`:

1. Create TempDir and db_path
2. Create valid database with indexed file containing pub fn existing_func() {}
3. Run `splice find --db <db_path> --name nonexistent_function`
4. Verify exit code is 1 (error) - symbol not found is a soft error
5. Parse stderr JSON
6. Validate:
   - status == "error"
   - error.error_code.code == "SPL-E001" (symbol resolution error) or appropriate code
   - error.message contains "nonexistent_function" or "not found"
7. Verify the error is NOT SPL-E091 (this is not a Magellan error, it's a Splice resolution error)

This distinguishes Magellan errors (SPL-E091) from Splice business logic errors (SPL-E001).
  </action>
  <verify>cargo test test_symbol_not_found_error_code passes</verify>
  <done>Symbol not found errors use correct SPL-E001 code, not SPL-E091</done>
</task>

<task type="auto">
  <name>Task 4: Add exit code mapping validation test</name>
  <files>tests/cli_tests.rs</files>
  <action>
Add test `test_exit_code_mapping_completeness`:

1. Test all exit code paths from SpliceExitCode enum:
2. Create test matrix for exit codes 0-5:
   - Exit code 0 (success): Run `splice status --db <valid_db>` -> expect 0
   - Exit code 1 (error): Run `splice find --db <valid_db> --name nonexistent` -> expect 1
   - Exit code 2 (usage): Run `splice find` (no --db flag) -> expect 2
   - Exit code 3 (database): Run `splice status --db /nonexistent.db` -> expect 3
   - Exit code 4 (file not found): Run `splice query --db <valid_db> --file /nonexistent.rs` -> expect 4
   - Exit code 5 (validation): Run `splice delete --file <file> --symbol test --strict` with invalid context -> expect 5
3. For each exit code, verify:
   - output.status.code() matches expected value
   - Error responses include error_code field
   - Success responses have status == "ok"

This validates the complete SpliceExitCode::from_error() mapping from main.rs.

Note: Some exit codes may require specific test setup; skip any that can't be reliably tested.
  </action>
  <verify>cargo test test_exit_code_mapping_completeness passes</verify>
  <done>All SpliceExitCode values (0-5) are correctly mapped from error conditions</done>
</task>

</tasks>

<verification>
Run all error code tests:
```bash
cargo test test_magellan_ cargo test test_symbol_not_found cargo test test_exit_code_mapping
```

Expected: All 4 new error code tests pass.
</verification>

<success_criteria>
- [ ] test_magellan_database_error_maps_to_spl_e091 passes
- [ ] test_magellan_query_error_preserves_context passes
- [ ] test_symbol_not_found_error_code passes
- [ ] test_exit_code_mapping_completeness passes
- [ ] SPL-E091 code is correctly generated for Magellan errors
- [ ] Exit codes 0-5 are correctly mapped for their respective error conditions
- [ ] No regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/26-integration-testing/26-03-SUMMARY.md`
</output>
