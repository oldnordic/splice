---
phase: 26-integration-testing
plan: 05
type: execute
wave: 2
depends_on: [26-01]
files_modified: [tests/cli_tests.rs]
autonomous: true

must_haves:
  truths:
    - "Query commands complete within acceptable time limits for typical codebase sizes"
    - "Database statistics query (status) is fast (< 100ms for 100 files)"
    - "Symbol query by labels scales linearly with file count"
    - "Find by name performance is documented for large codebases"
    - "Export command performance is acceptable for typical export sizes"
  artifacts:
    - path: "tests/cli_tests.rs"
      provides: "Performance benchmark tests for query commands"
      min_lines: 200
  key_links:
    - from: "tests/cli_tests.rs"
      to: "MagellanIntegration query methods"
      via: "Performance timing measurements"
      pattern: "Instant::now.*query.*elapsed"
    - from: "tests/cli_tests.rs"
      to: "query command performance"
      via: "CLI subprocess timing"
      pattern: "Command::output.*expect.*duration"
---

<objective>
Performance benchmarks - validate query performance is within acceptable limits

Purpose: Measure and document query command performance to ensure acceptable latency for typical codebase sizes
Output: 4 new performance tests in tests/cli_tests.rs with timing assertions and performance baseline documentation
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/26-integration-testing/26-RESEARCH.md

# Reference existing performance test patterns
@tests/performance_context_tests.rs
@tests/performance_relationship_tests.rs
@tests/relationship_performance.rs

# Reference query commands being benchmarked
@src/graph/magellan_integration.rs
@src/cli/mod.rs
@src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add status command performance benchmark</name>
  <files>tests/cli_tests.rs</files>
  <action>
Add test `test_benchmark_status_command_performance`:

1. Create TempDir and db_path
2. Index varying numbers of files (10, 50, 100 files):
   - Each file contains ~5 symbols (functions, structs)
3. For each file count:
   - Start timer
   - Run `splice status --db <db_path>` via Command
   - Measure elapsed time
   - Assert: 10 files < 50ms, 50 files < 200ms, 100 files < 500ms
4. Verify all commands return exit code 0
5. Document performance baseline in test comments

Use std::time::Instant for timing. Thresholds are based on typical CLI expectations (sub-second for interactive use).
  </action>
  <verify>cargo test test_benchmark_status_command_performance passes</verify>
  <done>Status command completes within acceptable time limits (500ms for 100 files)</done>
</task>

<task type="auto">
  <name>Task 2: Add query command performance benchmark</name>
  <files>tests/cli_tests.rs</files>
  <action>
Add test `test_benchmark_query_command_performance`:

1. Create TempDir and db_path
2. Index 100 Rust files with mixed symbols:
   - Functions, structs, impls, methods
3. Query performance tests:
   - Label-only query: `--label rust` (should be fast, uses label index)
   - Multi-label query: `--label rust --label fn` (AND semantics)
   - File-specific query: `--file <path>` (single file lookup)
4. For each query type:
   - Run 10 iterations
   - Measure average latency
   - Assert: average < 100ms for all query types
5. Verify result counts are correct
6. Document performance characteristics:
   - Label queries use index (O(log n))
   - File queries are direct lookup (O(1))

Reference MagellanIntegration::query_by_labels() for implementation details.
  </action>
  <verify>cargo test test_benchmark_query_command_performance passes</verify>
  <done>Query command completes within acceptable time limits (100ms average for indexed queries)</done>
</task>

<task type="auto">
  <name>Task 3: Add find command performance benchmark</name>
  <files>tests/cli_tests.rs</files>
  <action>
Add test `test_benchmark_find_command_performance`:

1. Create TempDir and db_path
2. Index 100 files with duplicate symbol names (simulating real codebases)
3. Find by name performance:
   - Unique symbol: `--name unique_function_name`
   - Common symbol: `--name process --ambiguous`
   - By symbol_id: `--symbol-id <16-char-hex>` (if implemented)
4. For each find type:
   - Run 10 iterations
   - Measure average latency
   - Assert: by name < 200ms, by symbol_id < 50ms (if available)
5. Document that find by name is O(N) where N = number of files
   (Magellan has no global symbol index, must query each file)

Reference MagellanIntegration::find_symbol_by_name() for O(N) implementation note.
  </action>
  <verify>cargo test test_benchmark_find_command_performance passes</verify>
  <done>Find command performance characteristics documented and within acceptable limits</done>
</task>

<task type="auto">
  <name>Task 4: Add export command performance benchmark</name>
  <files>tests/cli_tests.rs</files>
  <action>
Add test `test_benchmark_export_command_performance`:

1. Create TempDir and db_path
2. Index 100 files with ~500 symbols total
3. Export performance tests:
   - JSON export: `--format json --file export.json`
   - JSONL export: `--format jsonl --file export.jsonl`
   - CSV export: `--format csv --file export.csv`
4. For each format:
   - Run 5 iterations
   - Measure total export time
   - Measure output file size
   - Assert: export completes < 1 second for 500 symbols
5. Validate output file is valid (parse JSON/JSONL, verify CSV structure)
6. Document throughput: symbols/second for each format

Note: Export reads first 100 files for memory safety (see Phase 25-03). This is a documented limitation.
  </action>
  <verify>cargo test test_benchmark_export_command_performance passes</verify>
  <done>Export command completes within acceptable time limits (1s for 500 symbols)</done>
</task>

</tasks>

<verification>
Run all performance benchmarks:
```bash
cargo test test_benchmark_
```

Expected: All 4 performance tests pass with measured times within thresholds.
</verification>

<success_criteria>
- [ ] test_benchmark_status_command_performance passes
- [ ] test_benchmark_query_command_performance passes
- [ ] test_benchmark_find_command_performance passes
- [ ] test_benchmark_export_command_performance passes
- [ ] Performance baselines documented in test comments
- [ ] All operations complete within documented thresholds
- [ ] Performance characteristics (O(N) vs O(log n)) documented
</success_criteria>

<output>
After completion, create `.planning/phases/26-integration-testing/26-05-SUMMARY.md`
</output>
