---
phase: 26-integration-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/cli_tests.rs]
autonomous: true

must_haves:
  truths:
    - "User can run 'splice status' command to see database statistics"
    - "User can run 'splice query' command to list symbols in a file"
    - "User can run 'splice find' command to locate symbols by name or symbol_id"
    - "User can run 'splice refs' command to show callers/callees for a symbol"
    - "User can run 'splice files' command to list indexed files"
    - "All query commands support --output json flag for structured output"
    - "All query commands return correct exit codes (0-5 per Magellan conventions)"
  artifacts:
    - path: "tests/cli_tests.rs"
      provides: "End-to-end CLI subprocess tests for query commands"
      min_lines: 1800
    - path: "tests/magellan_integration_tests.rs"
      provides: "In-process Magellan wrapper tests (already exists, reference only)"
  key_links:
    - from: "tests/cli_tests.rs"
      to: "splice binary"
      via: "std::process::Command"
      pattern: "Command::new.*splice.*arg.*status"
    - from: "tests/cli_tests.rs"
      to: "MagellanIntegration::open()"
      via: "In-process database fixture setup"
      pattern: "MagellanIntegration::open"
    - from: "tests/cli_tests.rs"
      to: "serde_json::from_str"
      via: "JSON output validation"
      pattern: "serde_json::from_str.*extract_json_from_stdout"
---

<objective>
End-to-end integration tests for all Magellan query commands (status, query, find, refs, files)

Purpose: Validate that the unified CLI interface correctly executes all Magellan-delegated query commands with proper exit codes and JSON output
Output: 5 new integration tests in tests/cli_tests.rs covering status, query, find, refs, and files commands
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/26-integration-testing/26-RESEARCH.md

# Reference existing test patterns
@tests/cli_tests.rs
@tests/cli_output_tests.rs
@tests/magellan_integration_tests.rs

# Reference MagellanIntegration methods being tested
@src/graph/magellan_integration.rs
@src/cli/mod.rs
@src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add status command integration test</name>
  <files>tests/cli_tests.rs</files>
  <action>
Add test `test_query_status_command_returns_statistics`:

1. Create TempDir and db_path
2. Use MagellanIntegration::open() to create test database
3. Index a test Rust file (pub fn test_func() {})
4. Run `splice status --db <db_path>` via std::process::Command
5. Verify exit code is 0 (success)
6. Parse stdout with extract_json_from_stdout()
7. Validate StatusResponse structure:
   - files >= 1
   - symbols >= 1
   - db_path matches test database
8. Also test with --output json flag (same behavior)

Use existing get_splice_binary() and extract_json_from_stdout() helpers from cli_tests.rs.
  </action>
  <verify>cargo test test_query_status_command_returns_statistics passes</verify>
  <done>Status command returns database statistics in JSON format with exit code 0</done>
</task>

<task type="auto">
  <name>Task 2: Add query command integration test</name>
  <files>tests/cli_tests.rs</files>
  <action>
Add test `test_query_query_command_lists_symbols`:

1. Create TempDir and db_path
2. Create test file with multiple symbols:
   - pub fn helper() {}
   - pub fn main() { helper(); }
   - pub struct TestStruct;
3. Index via MagellanIntegration::open()
4. Run `splice query --db <db_path> --label rust --label fn --output json`
5. Verify exit code 0
6. Parse JSON, validate response contains symbols array
7. Verify symbols include "helper" and "main" functions
8. Verify Magellan field names (start_line, end_line) present in output
9. Test error case: query with non-existent label returns empty array (not error)

Use query_symbols_by_file() reference from magellan_integration.rs.
  </action>
  <verify>cargo test test_query_query_command_lists_symbols passes</verify>
  <done>Query command lists symbols with label filtering in JSON format</done>
</task>

<task type="auto">
  <name>Task 3: Add find command integration test</name>
  <files>tests/cli_tests.rs</files>
  <action>
Add test `test_query_find_command_locates_symbol`:

1. Create TempDir and db_path
2. Create test file: pub fn calculate(x: i32) -> i32 { x + 1 }
3. Index via MagellanIntegration::open()
4. Run `splice find --db <db_path> --name calculate --output json`
5. Verify exit code 0
6. Parse JSON, validate FindResponse structure
7. Verify symbols array contains exactly one symbol named "calculate"
8. Verify symbol has kind="fn", correct file_path, byte_start/byte_end

Also test --ambiguous flag with duplicate symbol names across files.
  </action>
  <verify>cargo test test_query_find_command_locates_symbol passes</verify>
  <done>Find command locates symbols by name with correct metadata</done>
</task>

<task type="auto">
  <name>Task 4: Add refs command integration test</name>
  <files>tests/cli_tests.rs</files>
  <action>
Add test `test_query_refs_command_shows_relationships`:

1. Create TempDir and db_path
2. Create test file with call relationship:
   ```rust
   pub fn caller() {
       callee();
   }
   pub fn callee() {}
   ```
3. Index via MagellanIntegration::open()
4. Run `splice refs --db <db_path> --path <file> --name callee --direction out --output json`
5. Verify exit code 0
6. Parse JSON, validate RefsResponse structure
7. Verify symbol.name == "callee"
8. Verify callees array contains reference to "caller"
9. Test --direction in (callers of callee)
10. Test --direction both (both callers and callees)

Use get_call_relationships() reference from magellan_integration.rs.
  </action>
  <verify>cargo test test_query_refs_command_shows_relationships passes</verify>
  <done>Refs command shows callers/callees with bidirectional support</done>
</task>

<task type="auto">
  <name>Task 5: Add files command integration test</name>
  <files>tests/cli_tests.rs</files>
  <action>
Add test `test_query_files_command_lists_indexed_files`:

1. Create TempDir and db_path
2. Create multiple test files (lib.rs, main.rs, helpers.rs)
3. Index all via MagellanIntegration::open()
4. Run `splice files --db <db_path> --output json`
5. Verify exit code 0
6. Parse JSON, validate FilesResponse structure
7. Verify files array contains all 3 test files
8. Verify each file has path and hash fields

Test with --symbols flag: verify symbol_count field present per file.
  </action>
  <verify>cargo test test_query_files_command_lists_indexed_files passes</verify>
  <done>Files command lists indexed files with optional symbol counts</done>
</task>

<task type="auto">
  <name>Task 6: Add query command error handling tests</name>
  <files>tests/cli_tests.rs</files>
  <action>
Add test `test_query_error_codes_match_magellan_conventions`:

1. Test database error (nonexistent DB):
   - Run `splice status --db /nonexistent/path.db`
   - Verify exit code 3 (database error per Magellan conventions)
2. Test usage error (missing required args):
   - Run `splice find --db <db_path>` (no --name or --symbol-id)
   - Verify exit code 2 (usage error)
3. Test file not found:
   - Run `splice query --db <db_path> --file /nonexistent/file.rs`
   - Verify exit code 4 (file not found)

For each error case:
- Parse stderr JSON if available
- Verify status == "error"
- Verify error_code field present

Use SpliceExitCode values from main.rs as reference.
  </action>
  <verify>cargo test test_query_error_codes_match_magellan_conventions passes</verify>
  <done>All query commands return correct exit codes (0=success, 2=usage, 3=database, 4=file not found)</done>
</task>

</tasks>

<verification>
Run all query command tests:
```bash
cargo test test_query_
```

Expected: All 6 new tests pass, plus existing tests still pass.
</verification>

<success_criteria>
- [ ] test_query_status_command_returns_statistics passes
- [ ] test_query_query_command_lists_symbols passes
- [ ] test_query_find_command_locates_symbol passes
- [ ] test_query_refs_command_shows_relationships passes
- [ ] test_query_files_command_lists_indexed_files passes
- [ ] test_query_error_codes_match_magellan_conventions passes
- [ ] All existing tests still pass (no regressions)
- [ ] Total test count increases by 6 tests
</success_criteria>

<output>
After completion, create `.planning/phases/26-integration-testing/26-01-SUMMARY.md`
</output>
