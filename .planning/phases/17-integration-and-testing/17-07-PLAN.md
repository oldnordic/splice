---
phase: 17-integration-and-testing
plan: 07
type: execute
wave: 2
depends_on:
  - 17-05-PLAN.md
files_modified:
  - src/graph/magellan_integration.rs
  - src/graph/schema.rs
  - src/relationships/mod.rs
  - tests/magellan_alignment_tests.rs
  - tests/cli_tests.rs
  - docs/MAGELLAN_DB_SCHEMA_CROSSCHECK.md
autonomous: false
must_haves:
  truths:
    - "Splice can open Magellan DBs created with sqlitegraph schema v3"
    - "Splice query/get returns results for Magellan labels (rust, fn, etc.)"
    - "Splice handles Magellan edge casing (DEFINES) when reading relationships"
    - "Splice JSON output remains aligned with Magellan unified schema"
  artifacts:
    - path: "tests/magellan_alignment_tests.rs"
      provides: "Cross-tool compatibility coverage"
      min_tests: 3
  key_links:
    - from: "src/graph/magellan_integration.rs"
      to: "magellan::CodeGraph"
      via: "open()"
      pattern: "MagellanGraph::open"
    - from: "tests/magellan_alignment_tests.rs"
      to: "splice query"
      via: "std::process::Command"
      pattern: "splice query"
---

<objective>
Align Splice's Magellan integration to accept current Magellan sqlitegraph schema and label/edge conventions.

Purpose: Enable a single Magellan DB to be used seamlessly by Splice (query/get) without reindexing or label translation by users.
Output: Splice can open Magellan DBs (schema v3), query symbols by Magellan labels, and traverse Magellan edges.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/MAGELLAN_DB_SCHEMA_CROSSCHECK.md
@src/graph/magellan_integration.rs
@src/graph/schema.rs
</context>

<tasks>

<task type="auto">
  <name>Define compatibility strategy for Magellan DBs</name>
  <files>
    src/graph/magellan_integration.rs
    src/graph/schema.rs
  </files>
  <action>
Inspect Magellan schema and Splice integration points and define exact compatibility behavior:

1. Accept sqlitegraph schema version used by Magellan (currently v3).
2. Treat Magellan label conventions (language + kind labels like `rust`, `fn`) as valid filters.
3. Accept edge type casing for `DEFINES` and relationship edges.
4. Preserve Splice JSON schema alignment.

Capture decision points in code comments only where behavior changes are non-obvious.
  </action>
  <verify>
Manual review checklist:
- Splice can open a Magellan DB without schema version errors.
- Splice label queries work with Magellan label set.
- No JSON field regressions in output schema.
  </verify>
  <done>
Compatibility strategy agreed and ready for implementation.
  </done>
</task>

<task type="auto">
  <name>Implement compatibility adjustments</name>
  <files>
    src/graph/magellan_integration.rs
    src/graph/schema.rs
    src/relationships/mod.rs
  </files>
  <action>
Implement minimal changes needed to read Magellan DBs:

- Accept current sqlitegraph schema version when opening Magellan DBs.
- Add label translation or acceptance so `rust`/`fn` style labels return symbols.
- Allow `DEFINES` edge casing when resolving relationships.

Keep changes isolated to Magellan integration layer if possible.
  </action>
  <verify>
Run targeted tests:

```bash
cargo test --test magellan_alignment_tests
cargo test --test cli_tests test_cli_query_magellan_flags
```
  </verify>
  <done>
Splice can query/get from Magellan DBs without schema errors.
  </done>
</task>

<task type="auto">
  <name>Add compatibility tests</name>
  <files>
    tests/magellan_alignment_tests.rs
    tests/cli_tests.rs
  </files>
  <action>
Add tests that:
- open a Magellan DB created by Magellan tooling
- query by Magellan labels (rust + fn) and assert non-empty results
- ensure `DEFINES` casing is handled for file symbol listings

Use existing Magellan test helpers and CLI test patterns.
  </action>
  <verify>
```bash
cargo test --test magellan_alignment_tests
cargo test --test cli_tests test_cli_query_magellan_flags
```
  </verify>
  <done>
Tests pass and cover the schema/label/edge compat paths.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Splice opens Magellan DBs and returns results using Magellan label/edge conventions.
  </what-built>
  <how-to-verify>
1. Run Magellan indexing on a temp project to produce a DB.
2. Run:
   ```bash
   splice query --db <magellan.db> -l rust -l fn --json --with-context --context-lines 1
   splice get --db <magellan.db> --file <file> --start <start> --end <end> --json
   ```
3. Confirm JSON output uses unified schema and results are non-empty.
  </how-to-verify>
  <resume-signal>
Type "approved" to proceed or describe what failed.
  </resume-signal>
</task>

</tasks>
