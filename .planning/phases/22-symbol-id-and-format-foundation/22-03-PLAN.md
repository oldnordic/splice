---
phase: 22-symbol-id-and-format-foundation
plan: 03
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - src/execution/base.rs
  - src/execution/mod.rs
autonomous: true

must_haves:
  truths:
    - "Execution IDs follow {timestamp_hex}-{pid_hex} format for delegated queries"
    - "Existing execution_id fields continue to use UUID format for backward compatibility"
    - "New generate_delegated_execution_id() function produces Magellan-compatible format"
  artifacts:
    - path: "src/execution/base.rs"
      provides: "Delegated execution ID generation function"
      contains: "generate_delegated_execution_id"
      min_lines: 20
  key_links:
    - from: "src/execution/base.rs"
      to: "src/symbol_id.rs"
      via: "uses generate_execution_id from symbol_id module"
      pattern: "crate::symbol_id::generate_execution_id"
    - from: "src/execution/mod.rs"
      to: "src/execution/base.rs"
      via: "re-exports generate_delegated_execution_id"
      pattern: "pub use.*generate_delegated_execution_id"
---

<objective>
Add delegated execution ID generation function for Magellan query delegation.

Purpose: Provide Magellan-compatible execution IDs when delegating queries, while maintaining UUID format for existing operations.

Output: Updated `src/execution/base.rs` with `generate_delegated_execution_id()` function.
</objective>

<execution_context>
@/home/feanor/.claude/get-shit-done/workflows/execute-plan.md
@/home/feanor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/execution/base.rs
@src/symbol_id.rs
@.planning/phases/22-symbol-id-and-format-foundation/22-01-PLAN.md
@.planning/phases/22-symbol-id-and-format-foundation/22-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Add generate_delegated_execution_id() to execution/base.rs</name>
  <files>src/execution/base.rs</files>
  <action>
Add a new function to `src/execution/base.rs`:

1. **Add use statement** at top: `use crate::symbol_id;`

2. **generate_delegated_execution_id() function**:
   - Calls `symbol_id::generate_execution_id()` to get Magellan-compatible format
   - Signature: `pub fn generate_delegated_execution_id() -> String`
   - Documentation: "Generate a Magellan-compatible execution ID for delegated queries. Format: {timestamp_hex}-{pid_hex}"
   - Place after the ExecutionLogBuilder impl (after line 252)

3. **DO NOT modify existing UUID-based execution_id generation** - preserve backward compatibility

4. **Add module documentation update**: Note that delegated queries use different ID format than UUID operations

Reference existing pattern in src/output.rs:124-145 (OperationResult::with_execution_id).
  </action>
  <verify>cargo check --lib execution::base 2>&1 | head -15</verify>
  <done>
cargo check confirms the function compiles and is accessible via the execution module.
  </done>
</task>

<task type="auto">
  <name>Re-export generate_delegated_execution_id from execution/mod.rs</name>
  <files>src/execution/mod.rs</files>
  <action>
Update `src/execution/mod.rs`:
1. Add re-export: `pub use base::generate_delegated_execution_id;`
2. Place alongside existing pub use statements
3. Verify it follows the existing pattern for other base exports
  </action>
  <verify>cargo check --lib 2>&1 | grep -i execution || echo "Execution module check passed"</verify>
  <done>
The function is properly re-exported from execution module for use by other modules.
  </done>
</task>

<task type="auto">
  <name>Add unit tests for delegated execution ID</name>
  <files>src/execution/base.rs</files>
  <action>
Add unit tests to the existing `#[cfg(test)]` module in execution/base.rs:

1. `test_delegated_execution_id_format`: Verify format matches {8-char-hex}-{4-char-hex} using regex
2. `test_delegated_execution_id_uniqueness`: Generate multiple IDs, verify they're unique
3. `test_delegated_execution_id_timestamp_valid`: Verify timestamp part is reasonable (within last 60 seconds)
4. `test_delegated_execution_id_pid_valid`: Verify pid part matches current process

Use existing test patterns from base.rs tests (lines 340-463) as reference.
  </action>
  <verify>cargo test --lib execution::base::tests::test_delegated 2>&1</verify>
  <done>
All 4 new unit tests pass, validating delegated execution ID format and uniqueness.
  </done>
</task>

</tasks>

<verification>
Phase verification criteria:
1. `cargo check --lib` passes without errors
2. `cargo test --lib execution::base` shows all tests passing (including new tests)
3. Delegated execution IDs match {timestamp_hex}-{pid_hex} format
4. Existing UUID-based execution_id generation is unchanged
</verification>

<success_criteria>
1. generate_delegated_execution_id() function exists in execution/base.rs
2. Function is re-exported from execution/mod.rs
3. All unit tests pass (including new delegated execution ID tests)
4. Existing tests continue to pass (backward compatibility)
</success_criteria>

<output>
After completion, create `.planning/phases/22-symbol-id-and-format-foundation/22-03-SUMMARY.md`
</output>
