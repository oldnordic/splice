---
phase: 22-symbol-id-and-format-foundation
plan: 04
type: execute
wave: 2
depends_on: ["22-01", "22-02", "22-03"]
files_modified:
  - tests/format_compatibility_tests.rs
  - tests/id_format_tests.rs
autonomous: true

must_haves:
  truths:
    - "JSON schema compatibility tests verify format alignment with Magellan"
    - "Symbol IDs are 16-char hex format"
    - "Execution IDs for delegated queries match {timestamp_hex}-{pid_hex} format"
    - "Field translation works bidirectionally between Magellan and Splice formats"
  artifacts:
    - path: "tests/format_compatibility_tests.rs"
      provides: "JSON schema compatibility tests for Magellan alignment"
      min_lines: 80
      contains: "test_magellan_span_schema", "test_field_translation"
    - path: "tests/id_format_tests.rs"
      provides: "ID format validation tests"
      min_lines: 100
      contains: "test_symbol_id_format", "test_execution_id_format"
  key_links:
    - from: "tests/format_compatibility_tests.rs"
      to: "src/format/magellan.rs"
      via: "tests field translation functions"
      pattern: "crate::format::"
    - from: "tests/id_format_tests.rs"
      to: "src/symbol_id.rs"
      via: "tests symbol ID generation"
      pattern: "splice::symbol_id::"
    - from: "tests/id_format_tests.rs"
      to: "src/execution/base.rs"
      via: "tests execution ID generation"
      pattern: "splice::execution::generate_delegated_execution_id"
---

<objective>
Create JSON schema compatibility tests validating Magellan format alignment.

Purpose: Verify that Splice's output formats align with Magellan conventions for interoperability.

Output: Integration tests for format compatibility and ID format validation.
</objective>

<execution_context>
@/home/feanor/.claude/get-shit-done/workflows/execute-plan.md
@/home/feanor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/format/magellan.rs
@src/symbol_id.rs
@src/execution/base.rs
@src/output.rs
@.planning/phases/22-symbol-id-and-format-foundation/22-01-SUMMARY.md
@.planning/phases/22-symbol-id-and-format-foundation/22-02-SUMMARY.md
@.planning/phases/22-symbol-id-and-format-foundation/22-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create tests/id_format_tests.rs for ID format validation</name>
  <files>tests/id_format_tests.rs</files>
  <action>
Create `tests/id_format_tests.rs` with comprehensive ID format validation tests:

1. **Module imports**: `use splice::symbol_id;` and `use splice::execution;`

2. **Symbol ID format tests**:
   - `test_symbol_id_format`: Verify exactly 16 lowercase hex chars via regex `^[0-9a-f]{16}$`
   - `test_symbol_id_deterministic`: Same inputs produce identical IDs across multiple calls
   - `test_symbol_id_unique_different_inputs`: Different inputs produce different IDs
   - `test_symbol_id_components`: ID changes when any component (name, file_path, byte_start) changes
   - `test_symbol_id_unicode`: Test with Unicode symbol names (verify UTF-8 handling)
   - `test_symbol_id_edge_cases`: Empty string, very long names, special characters

3. **Execution ID format tests**:
   - `test_execution_id_format`: Verify {timestamp_hex}-{pid_hex} format via regex `^[0-9a-f]{8}-[0-9a-f]{4}$`
   - `test_execution_id_timestamp_valid`: Timestamp is within reasonable range (last 60 seconds)
   - `test_execution_id_pid_matches`: PID matches std::process::id()
   - `test_execution_id_uniqueness`: Multiple calls produce unique IDs
   - `test_delegated_execution_id_exported`: Verify function is accessible from splice::execution

4. **Use existing test patterns**: Reference tests/cross_language_rich_span_tests.rs for test structure

5. **Add documentation**: Module-level docs explaining these tests validate Magellan compatibility
  </action>
  <verify>cargo test --test id_format_tests 2>&1</verify>
  <done>
All 11 ID format tests pass, validating Magellan-compatible ID generation for symbols and delegated executions.
  </done>
</task>

<task type="auto">
  <name>Create tests/format_compatibility_tests.rs for schema validation</name>
  <files>tests/format_compatibility_tests.rs</files>
  <action>
Create `tests/format_compatibility_tests.rs` with Magellan schema compatibility tests:

1. **Module imports**:
   - `use splice::format::{MagellanSpan, from_magellan, to_magellan, translate_field_name};`
   - `use splice::output::{Span, SpanResult};`

2. **Field translation tests**:
   - `test_from_magellan_translation`: Verify start_line -> line_start mapping for all 4 fields
   - `test_to_magellan_translation`: Verify line_start -> start_line mapping for all 4 fields
   - `test_roundtrip_preserves_data`: SpliceSpan -> MagellanSpan -> SpliceSpan preserves all fields
   - `test_translate_field_name_mapping`: Test all 4 field name translations

3. **JSON schema compatibility tests**:
   - `test_magellan_span_serializable`: MagellanSpan serializes to valid JSON
   - `test_magellan_span_field_names`: Serialized JSON uses Magellan field names (start_line, etc.)
   - `test_splice_span_field_names`: SpliceSpan uses Splice field names (line_start, etc.)

4. **Integration with existing types**:
   - `test_spanresult_to_magellan`: Convert SpanResult to MagellanSpan format
   - `test_spanresult_field_ordering`: Verify fields are in consistent order for serialization

5. **Optional fields handling**:
   - `test_optional_fields_preserved_translation`: context, semantics, relationships survive translation
   - `test_checksums_preserved`: checksum data survives roundtrip

6. **Add documentation**: Module-level docs explaining these tests ensure Magellan compatibility

Reference existing test patterns from tests/llm_consumption_tests.rs for structure.
  </action>
  <verify>cargo test --test format_compatibility_tests 2>&1</verify>
  <done>
All 12 format compatibility tests pass, validating JSON schema alignment with Magellan conventions.
  </done>
</task>

</tasks>

<verification>
Phase verification criteria:
1. `cargo test --test id_format_tests` shows all ID format tests passing
2. `cargo test --test format_compatibility_tests` shows all schema compatibility tests passing
3. All existing tests continue to pass (`cargo test --lib`)
4. Symbol IDs are verified to be 16-char lowercase hex format
5. Execution IDs for delegated queries match {timestamp_hex}-{pid_hex} format
6. Field translation works bidirectionally
</verification>

<success_criteria>
1. tests/id_format_tests.rs exists with 100+ LOC and 11 tests
2. tests/format_compatibility_tests.rs exists with 80+ LOC and 12 tests
3. All new tests pass
4. All existing tests pass (no regressions)
5. JSON output validates against expected schemas
</success_criteria>

<output>
After completion, create `.planning/phases/22-symbol-id-and-format-foundation/22-04-SUMMARY.md`
</output>
