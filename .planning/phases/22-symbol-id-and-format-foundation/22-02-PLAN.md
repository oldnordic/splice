---
phase: 22-symbol-id-and-format-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/format/mod.rs
  - src/format/magellan.rs
  - src/lib.rs
autonomous: true

must_haves:
  truths:
    - "Field translation utilities convert between Magellan (start_line) and Splice (line_start) conventions"
    - "Translation functions handle all 4 span fields (start_line, end_line, start_col, end_col)"
    - "Format module is properly exported and accessible"
  artifacts:
    - path: "src/format/mod.rs"
      provides: "Format module declaration and re-exports"
      min_lines: 10
      contains: "pub mod magellan"
    - path: "src/format/magellan.rs"
      provides: "Field translation utilities for Magellan compatibility"
      min_lines: 120
      contains: "MagellanSpan", "from_magellan", "to_magellan"
  key_links:
    - from: "src/format/magellan.rs"
      to: "src/output.rs"
      via: "uses SpanResult type for field mapping"
      pattern: "crate::output::SpanResult"
    - from: "src/lib.rs"
      to: "src/format/mod.rs"
      via: "pub mod format"
      pattern: "pub mod format"
---

<objective>
Create the format module with Magellan field translation utilities.

Purpose: Handle field name differences between Magellan (start_line) and Splice (line_start) conventions with bidirectional conversion.

Output: New `src/format/` module with `mod.rs` and `magellan.rs` files.
</objective>

<execution_context>
@/home/feanor/.claude/get-shit-done/workflows/execute-plan.md
@/home/feanor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/output.rs
@src/graph/magellan_integration.rs
@.planning/phases/22-symbol-id-and-format-foundation/22-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Create src/format/magellan.rs with field translation utilities</name>
  <files>src/format/magellan.rs</files>
  <action>
Create a new module `src/format/magellan.rs` with the following:

1. **MagellanSpan struct**: Mirror of Span but with Magellan field names
   - Fields: span_id, file_path, byte_start, byte_end, start_line, end_line, start_col, end_col
   - Add derives: Debug, Clone, Serialize, Deserialize
   - All fields match Magellan's naming convention (start_line not line_start)

2. **SpliceSpan struct alias**: Type alias for clarity
   - `pub type SpliceSpan = crate::output::Span;`

3. **from_magellan() function**: Convert MagellanSpan to SpliceSpan
   - Maps: start_line -> line_start, end_line -> line_end, start_col -> line_start, end_col -> line_end
   - Signature: `pub fn from_magellan(span: MagellanSpan) -> SpliceSpan`

4. **to_magellan() function**: Convert SpliceSpan to MagellanSpan
   - Maps: line_start -> start_line, line_end -> end_line, col_start -> start_col, col_end -> end_col
   - Signature: `pub fn to_magellan(span: SpliceSpan) -> MagellanSpan`

5. **translate_field_name() function**: Static utility for field name translation
   - Returns Magellan equivalent for Splice field names
   - Signature: `pub fn translate_field_name(splice_field: &str) -> Option<&'static str>`

6. **Module documentation**: Add docs explaining field name differences and translation rationale

Reference existing Span struct in src/output.rs:456-544 for field definitions.
  </action>
  <verify>cargo check --lib format::magellan 2>&1 | head -20</verify>
  <done>
cargo check confirms the format::magellan module compiles without errors, with proper field mapping between Magellan and Splice conventions.
  </done>
</task>

<task type="auto">
  <name>Create src/format/mod.rs with module exports</name>
  <files>src/format/mod.rs</files>
  <action>
Create `src/format/mod.rs` with:
1. Module declaration: `pub mod magellan;`
2. Re-export commonly used types:
   - `pub use magellan::{MagellanSpan, SpliceSpan, from_magellan, to_magellan, translate_field_name};`
3. Module-level documentation explaining the format module's purpose
  </action>
  <verify>grep -c "pub use" src/format/mod.rs 2>/dev/null || echo "0"</verify>
  <done>src/format/mod.rs exists with magellan module and pub use declarations.
  </done>
</task>

<task type="auto">
  <name>Add format module to lib.rs</name>
  <files>src/lib.rs</files>
  <action>
Add the format module declaration to src/lib.rs:
1. Find the module declarations section
2. Add `pub mod format;` in alphabetical order (after cli, before graph)
3. No other changes to lib.rs
  </action>
  <verify>cargo check --lib 2>&1 | grep -i format || echo "Format module check passed"</verify>
  <done>cargo check --lib completes without errors, showing the format module is properly exported.
  </done>
</task>

<task type="auto">
  <name>Add unit tests for field translation</name>
  <files>src/format/magellan.rs</files>
  <action>
Add comprehensive unit tests in a `#[cfg(test)]` module:
1. `test_from_magellan_translation`: Verify start_line -> line_start mapping
2. `test_to_magellan_translation`: Verify line_start -> start_line mapping
3. `test_roundtrip_translation`: Convert Splice -> Magellan -> Splice, verify equality
4. `test_translate_field_name`: Test field name translation utility
5. `test_optional_fields_preserved`: Verify context/semantics/relationships pass through
6. `test_span_id_preserved`: Verify span_id is unchanged in translation

Follow test patterns from src/graph/magellan_integration.rs:223-255.
  </action>
  <verify>cargo test --lib format::magellan::tests 2>&1</verify>
  <done>All 6 unit tests pass, validating bidirectional field translation between Magellan and Splice conventions.
  </done>
</task>

</tasks>

<verification>
Phase verification criteria:
1. `cargo check --lib` passes without errors
2. `cargo test --lib format::magellan` shows all tests passing
3. Field translation correctly maps start_line <-> line_start, end_line <-> line_end, start_col <-> col_start, end_col <-> col_end
4. Roundtrip conversion preserves data
</verification>

<success_criteria>
1. src/format/mod.rs exists with module declaration and re-exports
2. src/format/magellan.rs exists with 120+ LOC
3. from_magellan() and to_magellan() functions handle all 4 span fields
4. Module is exported via lib.rs
5. All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/22-symbol-id-and-format-foundation/22-02-SUMMARY.md`
</output>
