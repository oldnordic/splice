---
phase: 22-symbol-id-and-format-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/symbol_id.rs
  - src/lib.rs
autonomous: true

must_haves:
  truths:
    - "Symbol IDs are generated as 16-character hex strings (SHA-256, first 8 bytes)"
    - "ID generation is deterministic (same input produces same ID)"
    - "IDs are lowercase hexadecimal for consistency"
  artifacts:
    - path: "src/symbol_id.rs"
      provides: "16-char symbol ID generation functions"
      min_lines: 80
      contains: "generate_symbol_id", "SymbolId"
  key_links:
    - from: "src/symbol_id.rs"
      to: "sha2 crate"
      via: "use sha2::{Digest, Sha256}"
      pattern: "sha2::"
    - from: "src/lib.rs"
      to: "src/symbol_id.rs"
      via: "pub mod symbol_id"
      pattern: "pub mod symbol_id"
---

<objective>
Create the symbol ID generation module with Magellan-compatible 16-character hex format (SHA-256, first 8 bytes).

Purpose: Establish consistent symbol ID format that matches Magellan's convention for interoperability.

Output: New `src/symbol_id.rs` module with deterministic ID generation functions.
</objective>

<execution_context>
@/home/feanor/.claude/get-shit-done/workflows/execute-plan.md
@/home/feanor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/output.rs
@src/graph/magellan_integration.rs
@src/execution/base.rs
</context>

<tasks>

<task type="auto">
  <name>Create src/symbol_id.rs with 16-char hex ID generation</name>
  <files>src/symbol_id.rs</files>
  <action>
Create a new module `src/symbol_id.rs` with the following:

1. **SymbolId struct**: A newtype wrapper around String representing a 16-char hex ID
   - Add validation that ensures exactly 16 lowercase hex characters
   - Implement Display, Clone, PartialEq, Eq, Hash traits

2. **generate_symbol_id() function**: Generate deterministic 16-char hex ID from (name, file_path, byte_start)
   - Use SHA-256 hasher: `sha2::{Digest, Sha256}`
   - Hash format: `{name}:{file_path}:{byte_start}` (UTF-8 bytes)
   - Return first 8 bytes of hash as 16 lowercase hex characters
   - Signature: `pub fn generate_symbol_id(name: &str, file_path: &str, byte_start: usize) -> SymbolId`

3. **generate_execution_id() function**: Generate Magellan-compatible execution ID
   - Format: `{timestamp_hex}-{pid_hex}` where:
     - timestamp_hex: current Unix timestamp as 8-char lowercase hex
     - pid_hex: process ID as 4-char lowercase hex
   - Signature: `pub fn generate_execution_id() -> String`

4. **Module documentation**: Add module-level docs explaining the 16-char format rationale

Do NOT duplicate existing `generate_span_id()` from output.rs - this is for symbols, not spans.
  </action>
  <verify>cargo test --lib symbol_id 2>&1 | head -30</verify>
  <done>
Running cargo test on the symbol_id module shows tests passing, verifying:
- SymbolId struct validates 16-char hex format
- generate_symbol_id produces deterministic results (same input = same ID)
- generate_execution_id produces format {8-char-hex}-{4-char-hex}
  </done>
</task>

<task type="auto">
  <name>Add symbol_id module to lib.rs</name>
  <files>src/lib.rs</files>
  <action>
Add the module declaration to src/lib.rs:
1. Find the module declarations section
2. Add `pub mod symbol_id;` in alphabetical order
3. No other changes to lib.rs
  </action>
  <verify>cargo check --lib 2>&1 | head -10</verify>
  <done>cargo check --lib completes without errors, showing the symbol_id module is properly exported.
  </done>
</task>

<task type="auto">
  <name>Add unit tests for symbol_id module</name>
  <files>src/symbol_id.rs</files>
  <action>
Add comprehensive unit tests in a `#[cfg(test)]` module:
1. `test_symbol_id_valid_format`: Verify generated IDs are exactly 16 lowercase hex chars
2. `test_symbol_id_deterministic`: Same inputs produce identical IDs
3. `test_symbol_id_different_inputs`: Different inputs produce different IDs
4. `test_execution_id_format`: Verify {timestamp_hex}-{pid_hex} format with regex
5. `test_symbol_id_display`: Verify Display trait outputs correctly
6. `test_symbol_id_invalid_rejected`: Verify invalid format is rejected

Use existing test patterns from output.rs tests as reference.
  </action>
  <verify>cargo test --lib symbol_id::tests 2>&1</verify>
  <done>All 6 unit tests pass, validating:
- ID format correctness (16 chars, lowercase hex)
- Deterministic generation
- Format validation rejects invalid inputs
  </done>
</task>

</tasks>

<verification>
Phase verification criteria:
1. `cargo check --lib` passes without errors
2. `cargo test --lib symbol_id` shows all tests passing
3. Generated IDs are exactly 16 lowercase hex characters
4. ID generation is deterministic (verified by tests)
</verification>

<success_criteria>
1. src/symbol_id.rs module exists with 80+ LOC
2. generate_symbol_id() produces deterministic 16-char hex IDs
3. generate_execution_id() produces {timestamp_hex}-{pid_hex} format
4. Module is exported via lib.rs
5. All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/22-symbol-id-and-format-foundation/22-01-SUMMARY.md`
</output>
