---
phase: 05-span-aware-metadata
plan: 02
type: execute
depends_on: ["01"]
files_modified: [src/ingest/rust.rs, src/ingest/python.rs, src/ingest/cpp.rs, src/ingest/java.rs, src/ingest/javascript.rs, src/ingest/typescript.rs, src/graph/mod.rs]
autonomous: true
---

<objective>
Update all language ingest modules to compute and pass line/column metadata when storing symbols.

Purpose: Use the line/col storage infrastructure from Plan 01 to persist actual location data.
Output: All 7 language modules pass computed line/col to store_symbol_with_file_and_language.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Depends on Plan 01 which added line/col parameters to the storage function

@src/ingest/rust.rs
@src/ingest/python.rs
@src/ingest/cpp.rs
@src/ingest/java.rs
@src/ingest/javascript.rs
@src/ingest/typescript.rs
@src/graph/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Rust ingest module to pass line/col</name>
  <files>src/ingest/rust.rs</files>
  <action>The Rust module already computes line/col in extract_symbol() (lines 152-178). Update the symbol storage calls to pass these values.

Find where symbols are stored (likely in a separate function that calls graph.store_symbol_with_file_and_language). Add line_start, line_end, col_start, col_end parameters computed from the ropey conversion.

The conversion code already exists (lines 156-177):
```rust
let line_start = rope.char_to_line(start_char);
let line_end = rope.char_to_line(end_char);
// ... col computation
line_start: line_start + 1,  // 1-based
line_end: line_end + 1,
```

Pass these values to the storage function instead of discarding them.</action>
  <verify>cargo check passes, cargo test passes</verify>
  <done>Rust ingest module stores line/col with symbols</done>
</task>

<task type="auto">
  <name>Task 2: Update Python ingest module to pass line/col</name>
  <files>src/ingest/python.rs</files>
  <action>Similar to Rust, the Python module already computes line/col (lines 187-209). Update the storage call to pass line_start, line_end, col_start, col_end.

The Python module uses ropey for conversion at lines 187-195. Ensure these computed values are passed through to graph.store_symbol_with_file_and_language.</action>
  <verify>cargo check passes, cargo test ingest::python::tests passes</verify>
  <done>Python ingest module stores line/col with symbols</done>
</task>

<task type="auto">
  <name>Task 3: Update C/C++ ingest module to pass line/col</name>
  <files>src/ingest/cpp.rs</files>
  <action>The C/C++ module computes line/col at lines 368-408. Update storage calls to pass these values.

Note: C and C++ share this module. Ensure both language variants pass line/col correctly.</action>
  <verify>cargo check passes, cargo test ingest::cpp::tests passes</verify>
  <done>C/C++ ingest module stores line/col with symbols</done>
</task>

<task type="auto">
  <name>Task 4: Update Java ingest module to pass line/col</name>
  <files>src/ingest/java.rs</files>
  <action>The Java module computes line/col at lines 231-253. Update storage calls to pass these values.</action>
  <verify>cargo check passes, cargo test ingest::java::tests passes</verify>
  <done>Java ingest module stores line/col with symbols</done>
</task>

<task type="auto">
  <name>Task 5: Update JavaScript/TypeScript ingest modules to pass line/col</name>
  <files>src/ingest/javascript.rs, src/ingest/typescript.rs</files>
  <action>Both modules compute line/col using ropey:
- JavaScript: lines 245-268
- TypeScript: lines 264-287

Update storage calls in both modules to pass computed line/col values.</action>
  <verify>cargo check passes, cargo test ingest::javascript::tests ingest::typescript::tests pass</verify>
  <done>JavaScript and TypeScript ingest modules store line/col with symbols</done>
</task>

<task type="auto">
  <name>Task 6: Update CodeGraph integration tests</name>
  <files>src/graph/mod.rs</files>
  <action>Update or add integration tests that verify line/col are correctly stored for at least one language (Rust is sufficient as proof-point).

Create a test that:
1. Ingests a file with known symbol locations
2. Retrieves the symbol from graph
3. Asserts line_start, line_end, col_start, col_end match expected values</action>
  <verify>cargo test passes, integration test validates line/col storage</verify>
  <done>Integration test confirms line/col stored correctly</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo check passes
- [ ] All language ingest tests pass
- [ ] Integration test confirms line/col in graph
- [ ] No tests broken by changes
</verification>

<success_criteria>

- All 7 language modules pass line/col to storage
- Integration test confirms values stored in graph
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-span-aware-metadata/05-02-SUMMARY.md`
</output>
