---
phase: 18-error-code-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/error_codes.rs, src/cli/mod.rs]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "All CLI error paths produce structured error codes in JSON output"
    - "JSON error output includes explain_command field referencing splice explain"
    - "All 22 error-level SPL-E### variants are covered by SpliceError mappings"
    - "Error codes are consistent across all 7 language operations"
    - "splice explain command returns documentation for all error codes"
  artifacts:
    - path: "src/error_codes.rs"
      provides: "Error code mappings for all SpliceError variants"
      contains: "SpliceErrorCode::from_splice_error"
    - path: "src/cli/mod.rs"
      provides: "CLI error payload with explain_command field"
      exports: ["CliErrorPayload", "ErrorDetails"]
  key_links:
    - from: "src/cli/mod.rs"
      to: "src/error_codes.rs"
      via: "SpliceErrorCode::from_splice_error in CliErrorPayload::from_error"
      pattern: "SpliceErrorCode::from_splice_error"
    - from: "src/error_codes.rs"
      to: "SpliceError variants"
      via: "match statement mapping each variant to error code"
      pattern: "from_splice_error.*match"
---

<objective>
Map all remaining SpliceError variants to error codes and add explain_command field to error responses

Purpose: Complete the error code integration that was started in Phase 11 and Phase 15. Error code infrastructure exists (28 codes, with_error_code() builder, splice explain command) but some SpliceError variants return None from from_splice_error(), meaning they don't get error codes in CLI output.

Output: All error-level SpliceError variants map to SPL-E### codes, JSON errors include explain_command field
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/18-error-code-integration/18-CONTEXT.md

@src/error_codes.rs
@src/error.rs
@src/cli/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Map remaining SpliceError variants to error codes</name>
  <files>src/error_codes.rs</files>
  <action>
  Update SpliceErrorCode::from_splice_error() in src/error_codes.rs to map the remaining SpliceError variants:

  1. Add mappings for currently unmapped error-level variants:
     - InsufficientDiskSpace -> FileWriteError (SPL-E032)
     - InvalidDateFormat -> InvalidPlanSchema (SPL-E051) or add new code
     - QueryError -> DatabaseError (SPL-E062)
     - CargoCheckFailed -> CompilerValidationFailed (SPL-E043)
     - ExecutionRecordFailed -> ExecutionLogError (SPL-E071)

  2. Keep these intentionally unmapped (document why):
     - BrokenPipe - not a user-fixable error, stdout pipe closed is terminal
     - Utf8 (std lib variant) - covered by InvalidUtf8 variant
     - Other - generic catchall, no specific code applicable

  3. Add match arms before the final `_ => None` catchall

  The mapping should be complete such that all error-level SpliceError variants return Some(SpliceErrorCode).
  </action>
  <verify>cargo test error_code_from_splice_error --lib</verify>
  <done>All error-level SpliceError variants map to a SpliceErrorCode (Only BrokenPipe, Utf8, Other return None intentionally)</done>
</task>

<task type="auto">
  <name>Add explain_command field to ErrorDetails struct</name>
  <files>src/cli/mod.rs</files>
  <action>
  Add explain_command field to ErrorDetails struct in src/cli/mod.rs:

  1. Add new field to ErrorDetails struct (around line 594):
     ```rust
     /// Optional explain command for this error
     #[serde(skip_serializing_if = "Option::is_none")]
     pub explain_command: Option<String>,
     ```

  2. Update CliErrorPayload::from_error() to populate explain_command when error_code is present (around line 640):
     ```rust
     let explain_command = error_code.as_ref().map(|ec| {
         format!("splice explain {}", ec.code)
     });
     ```

  3. Add explain_command to the ErrorDetails construction in the return statement

  This follows the decision in 18-CONTEXT.md: "Error responses auto-reference the explain command" with format "splice explain SPL-E001"
  </action>
  <verify>cargo check</verify>
  <done>ErrorDetails struct includes explain_command field that is populated when error_code is present</done>
</task>

<task type="auto">
  <name>Add tests for error code coverage</name>
  <files>src/error_codes.rs</files>
  <action>
  Add comprehensive tests to verify all error-level SpliceError variants produce error codes:

  Add test function in src/error_codes.rs tests module:

  1. Test that all error-level SpliceError variants return Some(SpliceErrorCode):
     - Create instances of each SpliceError variant
     - Call SpliceErrorCode::from_splice_error() on each
     - Assert all return Some() except BrokenPipe, Utf8, Other

  2. Test that explain_command is generated for all error codes:
     - Verify format "splice explain SPL-E###"
     - Verify it's included in ErrorDetails when error_code is present

  3. Add test counting total mapped variants (should be 22 error-level codes)

  Place tests in existing #[cfg(test)] mod tests block at end of src/error_codes.rs
  </action>
  <verify>cargo test test_error_code_coverage --lib</verify>
  <done>All 22 error-level SpliceError variants map to error codes; BrokenPipe/Utf8/Other explicitly documented as unmapped</done>
</task>

</tasks>

<verification>
1. Run `cargo test --lib` to verify all error code tests pass
2. Run `cargo check` to ensure no compilation errors
3. Manually test JSON error output includes explain_command field
4. Verify `splice explain SPL-E###` works for all error codes
</verification>

<success_criteria>
1. All CLI error paths produce SPL-E### error codes in JSON output
2. JSON error output includes explain_command field with format "splice explain SPL-E###"
3. All 22 error-level SPL-E### variants are covered by SpliceError mappings
4. BrokenPipe, Utf8, Other variants are intentionally unmapped and documented
5. Error codes are consistent across all 7 language operations (no language-specific gaps)
</success_criteria>

<output>
After completion, create `.planning/phases/18-error-code-integration/18-01-SUMMARY.md`
</output>
