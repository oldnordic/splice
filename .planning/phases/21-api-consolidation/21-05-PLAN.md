---
phase: 21-api-consolidation
plan: 05
type: execute
wave: 2
depends_on: ["21-01", "21-02", "21-03", "21-04"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All unit tests pass (cargo test)"
    - "All integration tests pass"
    - "Cross-language tests pass for all 7 languages"
    - "No new compiler warnings introduced"
    - "Execution log tests verify config-based approach"
  artifacts:
    - path: "test output"
      provides: "Verification of all fixes"
  key_links:
    - from: "this plan"
      to: "all previous phase 21 plans"
      via: "testing consolidated code"
---

<objective>
Run comprehensive tests to verify all fixes from Phase 21 and confirm all 67 issues from bug analysis are addressed.

Purpose: After consolidating the API (parser_for_language, import extraction trait, resolve symbol API, execution log config), we need to verify that all changes work correctly and no regressions were introduced. This also provides the final verification that all 67 issues from the bug analysis have been addressed across Phases 19-21.

Output: Complete test results showing all tests pass, with documented verification status for each bug category.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/BUG_ANALYSIS.md

# Previous phase summaries for context
@.planning/phases/19-critical-error-handling/19-01-SUMMARY.md
@.planning/phases/19-critical-error-handling/19-02-SUMMARY.md
@.planning/phases/19-critical-error-handling/19-03-SUMMARY.md
@.planning/phases/19-critical-error-handling/19-04-SUMMARY.md
@.planning/phases/19-critical-error-handling/19-05-SUMMARY.md
@.planning/phases/19-critical-error-handling/19-06-SUMMARY.md
@.planning/phases/19-critical-error-handling/19-07-SUMMARY.md

@.planning/phases/20-lifetime-resource-safety/20-01-SUMMARY.md
@.planning/phases/20-lifetime-resource-safety/20-02-SUMMARY.md
@.planning/phases/20-lifetime-resource-safety/20-03-SUMMARY.md
@.planning/phases/20-lifetime-resource-safety/20-04-SUMMARY.md
@.planning/phases/20-lifetime-resource-safety/20-05-SUMMARY.md
@.planning/phases/20-lifetime-resource-safety/20-06-SUMMARY.md
@.planning/phases/20-lifetime-resource-safety/20-07-SUMMARY.md

# Phase 21 plans (must be complete)
@.planning/phases/21-api-consolidation/21-01-SUMMARY.md
@.planning/phases/21-api-consolidation/21-02-SUMMARY.md
@.planning/phases/21-api-consolidation/21-03-SUMMARY.md
@.planning/phases/21-api-consolidation/21-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite</name>
  <files></files>
  <action>
    Execute the complete test suite to verify all changes:

    1. Run `cargo clean` to ensure clean build
    2. Run `cargo test --all-targets` - capture full output
    3. Check for any failing tests
    4. Document any warnings (especially unused code warnings from refactoring)

    Capture the test statistics:
    - Total tests run
    - Tests passed
    - Tests failed (if any)
    - Compiler warnings
  </action>
  <verify>cargo test --all-targets completes with all tests passing</verify>
  <done>Full test suite passes with no failures</done>
</task>

<task type="auto">
  <name>Task 2: Run cross-language integration tests</name>
  <files></files>
  <action>
    Specifically test the import extraction and parser consolidation:

    1. Run `cargo test ingest::imports::` - verify all import extraction tests pass
    2. Run `cargo test cross_language` - verify multi-language tests pass
    3. Run `cargo test parser_for_language` - verify shared parser works
    4. Run `cargo test resolve::` - verify resolve API changes work

    These tests specifically exercise the code refactored in Phase 21.
  </action>
  <verify>
    cargo test ingest::imports:: passes
    cargo test cross_language passes
    cargo test resolve:: passes
  </verify>
  <done>All cross-language and import tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Run execution log config tests</name>
  <files></files>
  <action>
    Verify the new ExecutionLogConfig works correctly:

    1. Run `cargo test execution::log` - verify execution log tests pass
    2. Check that tests using ExecutionLogConfig::disabled() work without environment variable pollution
    3. Verify tests run correctly in parallel (no race conditions from env_lock)

    The refactored approach should eliminate the need for env_lock() in most tests.
  </action>
  <verify>cargo test execution::log passes with no race condition warnings</verify>
  <done>Execution log config tests pass without environment pollution</done>
</task>

<task type="auto">
  <name>Task 4: Verify no regressions from previous phases</name>
  <files></files>
  <action>
    Run specific test suites for fixes from Phases 19-20:

    1. Run `cargo test test_unwrap_safe` - verify unwrap() fixes from Phase 19
    2. Run `cargo test utf8` - verify UTF-8 handling from Phase 19
    3. Run `cargo test path` - verify path handling from Phase 20
    4. Run `cargo test env_lock` - verify environment variable handling from Phase 20

    Ensure no regressions occurred during API consolidation.
  </action>
  <verify>
    All phase 19 specific test patterns pass
    All phase 20 specific test patterns pass
  </verify>
  <done>No regressions from Phases 19-20 fixes</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Complete test run results for all Phase 21 API consolidations and verification of all previous bug fixes</what-built>
  <how-to-verify>
    1. Review the test output summary from Task 1
    2. Check that all 7 languages are tested and passing
    3. Verify the test count is reasonable (expect 340+ tests based on Phase 17)
    4. Confirm no new warnings were introduced
    5. Optionally run `cargo clippy` to check for additional lint issues

    Expected results:
    - All tests pass
    - No new warnings beyond existing ones
    - Test count >= 340 (from Phase 17 baseline)
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe any failures found</resume-signal>
</task>

</tasks>

<verification>
1. Full test suite passes: `cargo test --all-targets`
2. Import extraction tests pass: `cargo test ingest::imports::`
3. Resolve API tests pass: `cargo test resolve::`
4. Execution log tests pass: `cargo test execution::log`
5. No new compiler warnings introduced
6. No regressions from Phases 19-20 fixes
</verification>

<success_criteria>
1. All tests pass (100% success rate)
2. Test count >= 340 (maintains Phase 17 coverage)
3. No new warnings from API consolidation
4. All 7 language modules tested
5. Execution log config approach verified working
</success_criteria>

<output>
After completion, create `.planning/phases/21-api-consolidation/21-05-SUMMARY.md`
</output>
